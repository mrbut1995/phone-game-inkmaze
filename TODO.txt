# ============================================================================
# TODO — InkMaze / Number Maze
# ============================================================================
# Quy ước: [x] đã làm · [ ] chưa làm. Mỗi việc ghi kèm file liên quan.

# Replace SFX Placeholder to correct sound

# ----------------------------------------------------------------------------
# VÒNG SỬA 14 (2026-02): TÁCH HỆ HƯỚNG DẪN — PAGE · CONTENT · POPUP DÙNG CHUNG + NHÚNG HUD NGANG
# ----------------------------------------------------------------------------
# [x] LÀM theo yêu cầu user ("Tách các popup instruction ra, gộp lại thành popup_instruction
#     và content instruction") — 6 bước:
#     1. `nodes/popups/instruction/page.tscn` + `scripts/nodes/popups/instruction/page.gd`
#        (class `InstructionPage`): 1 TRANG gồm `Image` (label chữ-trên-ảnh PT# là CON của Image,
#        neo tỉ lệ) · `Title` · `Section` · `Instruction` (VBoxContainer các hàng luật) · `Cta`/`Link`.
#        Trang tự dàn 2 kiểu: khung RỘNG → ảnh trái + cột chữ phải; khung CAO → ảnh trên + stack dọc.
#     2. `nodes/popups/instruction/content_instruction.tscn` + `content_instruction.gd`
#        (class `InstructionContent`): Chip · Tabs · Pages · Prev/Next · Dots · Index + toàn bộ
#        hành vi lật trang (vuốt/lăn/phím/tab/dots), `set_embedded()`, signal `close_requested`.
#     3. 10 scene chế độ `nodes/popups/instruction/<mode>.tscn` GIỜ KẾ THỪA `content_instruction.tscn`
#        và dùng `Page1..3` là instance `page.tscn` (không còn kế thừa `base.tscn`/popup riêng).
#     4. `nodes/popups/popup_instruction.tscn` + `popup_instruction.gd` (class `PopupInstruction`):
#        POPUP dùng chung DUY NHẤT — nạp `<mode>.tscn` vào `Panel/Content` theo `{"mode_id": …}`,
#        tô giấy theo accent chế độ; thay toàn bộ 10 popup riêng cũ.
#     5. Nút "?" bố cục DỌC vẫn mở popup; HUD NGANG nhúng nội dung vào `Panel/GuideHost`
#        (`GameHUD.show_instruction_for` — full-rect, TRÀN kín khung; khung < 300×220 thì hiện nút
#        "XEM HƯỚNG DẪN" phát `instruction_requested` cho `GameScene` mở popup).
#     6. Generator `tools/mockup/gen_instruction_popups.py` ghi scene nửa cỡ (S=0.5), giữ uid +
#        unique_id, thêm `[editable]` cho 3 trang; hàng luật min 240×38 + label Title/Desc NEO PHẢI
#        để co được theo cột chữ khi nhúng vừa khung hẹp (trước đó hàng 377.5 cứng → tràn 66px).
#     - GameController: `INSTRUCTION_POPUP = res://nodes/popups/popup_instruction.tscn`;
#       `open_instruction()` → `Popups.open_path(INSTRUCTION_POPUP, {"mode_id": …})`;
#       `instruction_scene_path()` vẫn trả scene NỘI DUNG (dùng chung cho popup + HUD).
#     - XOÁ `scripts/nodes/popups/instruction_popup.gd` (class `InstructionPopup` cũ); `base.gd`
#       trả về nguyên trạng (bỏ phần `embedded` thử nghiệm); cập nhật comment `scripts/utils/popups.gd`.
#     - Test `scripts/test_case/test_instruction.gd` viết lại — **286 check** (page/content/popup/HUD);
#       full suite 13/13 + test_instruction PASS.
#     - KIỂM CHỨNG render bằng probe (ảnh ở `tmp_shot/instruction/`, đã dọn sau khi xem):
#       HUD ngang 1920×1080 nhúng dungeon TRÀN khung 812×493 · popup ngang + popup dọc (minesweeper)
#       đủ chip/tabs/hàng luật/CTA/link/dots/TRANG 1 / 3.
# [x] VÒNG 14b (2026-09-26) — GIAO DIỆN THEO MOCKUP `matchup_dungeon_landscape_with_instruction.svg`:
#     · CHỈ POPUP mới hiện Cta/Link (+Chip/Tabs/Title); bản NHÚNG ẩn hết và dời cụm
#       ‹ dots › TRANG vào hàng tiêu đề “LUẬT CHƠI” (trang cấp chỗ qua `nav_slot_rect()`);
#       `Pages` phủ KÍN khung khi nhúng (trước chỉ chiếm 772×319 vì còn băng tabs/nav cũ).
#     · Chữ trong KHUNG CHÚ THÍCH của mockup (comment CALLOUT / CHÚ THÍCH / DẢI) KHÔNG viết lên ảnh nữa:
#       generator thay bằng BADGE SỐ (`Point#`, màu theo chữ đầu của khung) + giữ chuỗi gốc trong
#       Label ẨN (`metadata/point` + `point_order`); `content_instruction.gd` đọc & đổ xuống khối mới
#       `Points/List/PointRow#` (NumCircle + Title = chữ ĐẦU + Desc = các chữ SAU nối “ · ”, autowrap).
#       Mockup nào cần đánh dấu điểm mới: viết comment `<!-- CALLOUT … -->` trước nhóm chữ đó.
#     · Bố cục trang theo mockup: NHÚNG = [ẢNH | ĐIỂM] hàng trên, hàng “LUẬT CHƠI” + điều hướng + luật ở dưới;
#       POPUP = ảnh (tự co theo chỗ còn lại) → Title → ĐIỂM → LUẬT CHƠI → Cta/Link.
#     · Chữ-trên-ảnh + badge TỰ CO theo cỡ ảnh thật (`metadata/base_font/base_size`) — hết tràn chữ.
#     · `test_instruction` **410 check** · full suite 13/13 · ảnh kiểm chứng ở `D:\godot-phone-game\tmp_shot_inkmaze\`
#       (`A_land_embed.png` · `B_land_popup_p1.png` · `C_port_popup_minesweeper.png`).

# [x] VÒNG 14c (2026-09-26) — SỬA LỖI THEO TỈ LỆ KHUNG: bố cục CỐ ĐỊNH bằng HBox/VBox
#     (user báo: “có aspect tỉ lệ như trên ảnh thì mảng chi tiết nằm dưới image → section trồi ra, paging lỗi”):
#     · NGUYÊN NHÂN: `page.gd` rẽ 2 nhánh theo `w >= h × 1.15` (khung RỘNG → [Ảnh|Điểm]; khung CAO → ảnh trên ·
#       ĐIỂM dưới) → ở tỉ lệ GẦN VUÔNG (khung Hướng dẫn của HUD ngang khi cửa sổ hẹp) rơi vào nhánh CAO ⇒ khối
#       ĐIỂM tụt xuống dưới ảnh, tràn khung + điều hướng (chỉ có công thức cho nhánh rộng) đặt lệch.
#     · SỬA GỐC: `page.tscn` cấu trúc CỐ ĐỊNH mọi trang — `Page → Title · Body (VBoxContainer)
#       { Top (HBoxContainer) { Image · Points } · Section · Instruction (VBox) }` + `Cta`/`Link`;
#       XOÁ hẳn nhánh wide/tall trong `page.gd` (chỉ còn 1 `_apply_layout()`).
#     · `page.gd` chỉ còn TÍNH: cỡ ẢNH theo tỉ lệ (`row_h = 48% bề cao Body`, kẹp 120–340;
#       `img_w = min(row_h × tỉ lệ, 52% bề ngang khối)` — tính từ bề cao VỪA ĐẶT của Body, không đọc `body.size`
#       vì lúc đó container chưa xếp xong); khối ĐIỂM nở lấp phần ngang còn lại + cấp min cao `26+min(List)+8`;
#       `Title` ẩn khi nhúng; `Cta/Link` dàn đáy — chừa chỗ CHỈ khi ở POPUP (theo cờ `embedded`, không dò `visible`).
#     · ĐIỀU HƯỚNG NHÚNG: `nav_slot_rect()` nay lấy y từ VỊ TRÍ THẬT của `Section`; trang phát lại `resized` SAU khi
#       container xếp xong (frame sau; CHỐNG LẶP bằng so cỡ) để `content_instruction.gd` đặt lại cụm ‹ dots › TRANG.
#     · ĐƯỜNG DẪN NODE ĐỔI (mọi nơi tra node phải theo cấu trúc mới): generator `gen_instruction_popups.py`
#       (`{ppath}/Body/Top`, `…/Body/Top/Image` cho PT#/badge, `…/Body/Top/Points/List`, `…/Body/Instruction`,
#       Title idx 0 · Cta idx 2 · Link idx 3) + `content_instruction.gd` (`Body/Top/Image`, `Body/Top/Points/List`)
#       + sinh lại 10 scene chế độ; `POINT_TEXT_MIN_W` 500 → **250** (cột ĐIỂM ở popup chỉ ~162 nên đặt min lớn hơn
#       sẽ đẩy tràn ngang).
#     · TEST: `test_instruction` **425 check** — thêm check CẤU TRÚC (`Body` VBox chứa `Top` HBox với Ảnh+Điểm) +
#       BẤT BIẾN qua 5 tỉ lệ khung (994×800 · 560×560 · 620×700 · 700×620 · 500×640: [Ảnh|Điểm] cùng hàng, khối ĐIỂM
#       gọn trong khung, `Section` ở dưới, điều hướng nằm hàng LUẬT CHƠI không tràn mép) · full suite 13/13.
#     · Ảnh kiểm chứng mới (cùng thư mục trên): `A_embed_1920x1080.png` · `B_embed_1440x1100.png` ·
#       `C_embed_1152x960.png` (tỉ lệ hẹp/gần vuông) · `D_popup_minesweeper_p1.png` · `E_popup_dungeon_p1.png` ·
#       `F_popup_minesweeper_p3_portrait.png`.

# [x] VÒNG 14d (2026-09-26) — SỬA 2 LỖI USER BÁO: “paging trôi lên trên khi lật trang” + “vẫn có chữ trên ảnh”:
#     · PAGING (nguyên nhân: `nav_slot_rect()` bám theo `Section` — trang CHƯA TỪNG hiện thì container chưa dàn
#       các con (Section nằm ở y≈13) ⇒ trang hiện LẦN ĐẦU đặt cụm điều hướng theo số cũ → NHẢY LÊN TRÊN):
#       nay điều hướng nằm trong **BĂNG ĐÁY khung** — `page.gd` chừa `NAV_BAND = 26` ở đáy `Body` (chỉ khi NHÚNG)
#       và `nav_slot_rect()` trả đúng băng đó ⇒ **DƯỚI khối “QUY TẮC …”** và vị trí CHỈ phụ thuộc CỠ TRANG
#       (lật trang không xê dịch). Lề nhúng `PAD` 14→10, nav `EMBED_NAV_BTN` 28→24, `EMBED_NAV_GAP` 8→6.
#     · TRÀN ĐÁY do min-size: bề cao tối thiểu của danh sách ĐIỂM (label autowrap) lúc chưa xếp xong có lúc
#       VỌT LÊN (200px) ⇒ min-size của `Body` vượt khung ⇒ kẹp tối thiểu → luật tràn đáy. SỬA: **TRẦN bề cao
#       khối ĐIỂM** = `page.gd::_points_budget()` (khung − lề − băng nav/khối hành động − khối luật − tiêu đề
#       mục); khung quá thấp thì khối ĐIỂM co lại + `clip_contents` trên Panel ĐIỂM cắt gọn.
#     · CHỮ TRÊN ẢNH: generator ẨN **MỌI chữ mô tả** (tiêu đề thẻ minh hoạ, nhãn công cụ, thẻ HUD giả…) — trước
#       đây chỉ chữ trong KHUNG CHÚ THÍCH bị ẩn, phần còn lại nhỏ li ti trên ảnh (nhất là P2/P3 của
#       normal_maze: “Ô SỐ 0 = AN TOÀN”, “BỊ BỊT KÍN 3 MẶT”, “HOÀN TÁC/GỢI Ý”…). Nay ảnh minh hoạ chỉ còn
#       HÌNH VẼ + SỐ/KÝ HIỆU (`is_literal_text`) + badge số của khối ĐIỂM; chữ nằm ở khối ĐIỂM + hàng luật.
#       (`build_pt(..., hidden=True)` ghi `visible = false`; số/ký hiệu vẫn vẽ thẳng như cũ.)
#     · TEST: `test_instruction` **435 check** — thêm “ảnh KHÔNG còn chữ mô tả” (mọi Label hiện trên ảnh đều là
#       số/ký hiệu) + check “điều hướng DƯỚI khối luật + KHÔNG xê dịch khi lật trang” (đo nav y ở trang 1 vs 2);
#       full suite 13/13. Ảnh kiểm chứng: `tmp_shot_inkmaze\{page1,page2,page3,popup_minesweeper_p1}.png`.

# [x] VÒNG 14e (2026-09-26) — TÁCH HẲN 2 KIỂU HIỂN THỊ ĐIỂM theo yêu cầu user (“trên scene thì ẩn chữ + hiện
#     point number + danh sách point row; trên popup thì hiện chữ trên image, KHÔNG point number/point section”):
#     · GENERATOR (`gen_instruction_popups.py`):
#       – `collect_points` gom **MỌI chữ mô tả** (không phải số/ký hiệu) thành ĐIỂM, theo comment mockup ĐỨNG
#         LIỀN TRƯỚC (comment trùng nhưng cách xa nhau vẫn tách nhóm; chữ không comment = mỗi chữ 1 điểm).
#         ⇒ nhiều điểm hơn trước (dày nhất sum_path P2 = 9; dungeon P1 = 6 — đúng ví dụ user nêu).
#       – `build_pt` gộp luôn việc gắn `metadata/point` + `point_order` (xoá `build_point_src`); Label chữ **HIỆN
#         MẶC ĐỊNH** (đúng bản POPUP) và có đủ vị trí/font/màu như mockup; `build_point_badge` thêm
#         `visible = false` (badge chỉ hiện ở bản NHÚNG).
#     · `page.tscn`: node `Points` thêm `visible = false` (mặc định = bản POPUP; script bật lên khi nhúng).
#     · `page.gd`: thêm `_apply_point_mode()` — NHÚNG: ẩn mọi Label có `metadata/point` + hiện badge (`base_size`);
#       POPUP: ngược lại. `_sync_points_visibility()` ⇒ `box.visible = embedded and có hàng`; khối ĐIỂM **chỉ
#       thuộc bản nhúng**. `_layout_image()` tách 2 công thức: nhúng giữ `img ≤ 52% bề ngang` (nhường cột ĐIỂM),
#       **popup ảnh nở HẾT bề ngang** (`img_w = min(usable, chỗ-cao-còn-lại × tỉ lệ)`); tách `_row_budget()`
#       dùng chung cho cỡ ảnh nhúng + TRẦN khối ĐIỂM (thay vì công thức trần riêng).
#     · TEST: `test_instruction` **478 check** — mỗi chế độ kiểm cả 2 chiều: POPUP (chữ hiện hết + badge ẨN +
#       khối ĐIỂM ẨN) → NHÚNG (`content.set_embedded(true)`: chữ ẩn hết MỌI trang + badge HIỆN + khối ĐIỂM HIỆN)
#       → trả lại POPUP (chữ hiện lại); popup thật + HUD nhúng cũng kiểm đúng chiều tương ứng; full suite 13/13.
#     · ĐO KHÔNG GIAN (probe tạm, đã xoá): khung GuideHost thật = 1614×598 @1920×1080 · 969×499 @1152×960 ·
#       836×366 @994×800; MỌI trang fit ở 2 cỡ đầu (dày nhất sum_path P2: cần 230 hàng-điểm ≤ 320 cấp);
#       khung 994×800 thì khối ĐIỂM kẹp theo trần ⇒ trang ≥7 điểm bị cắt hàng cuối trong panel (clip gọn, luật
#       vẫn nguyên) — chấp nhận: khung nhỏ ưu tiên LUẬT + ẢNH.
#     · Ảnh kiểm chứng (ngoài repo, `D:\godot-phone-game\tmp_shot_inkmaze\`): `embed_dungeon_p1.png` (badge + 6 hàng,
#       ảnh sạch chữ) · `embed_sumpath_p2.png` (9 hàng) · `embed_small_sumpath_p2.png` · `popup_dungeon_p1.png` ·
#       `popup_dungeon_p3.png` · `popup_minesweeper_p1.png` (chữ trên ảnh, không badge/không khối ĐIỂM).

# ----------------------------------------------------------------------------
# VÒNG SỬA 13 (2026-09-19): MÀN CHƠI — CONTROLLERS + BOARD DÙNG CHUNG (BỎ BỘ ĐÔI THEO HƯỚNG)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("tránh dùng 2 Controller và 2 Grid riêng biệt"):
#     1. `scenes/game.tscn` (scene gốc) nay chứa: 2 layout (`Portrait`, `Landscape`) + **`Board`**
#        (instance nodes/game/board.tscn) + **`Controllers`** (11 controller). Các `@export`
#        (game_controller, grid_controller, …) được gán bằng NodePath NGAY TRONG SCENE nên
#        mở scene là thấy liên kết, không phải tra bằng code.
#        · `GameController.grid_view` = `../../Board` · `GridController.board_view` = `../../Board`.
#        · `UIController.level_label/subtitle_label` GỠ khỏi scene (2 hướng đặt node khác chỗ:
#          dọc `Status/Title/…`, ngang `Side/Status/Title/…`) → GameScene tự gán mỗi lần đổi hướng.
#     2. `scenes/orientation/{portrait,landscape}/game.tscn` nay CHỈ còn UI: Status · Information
#        (HUD) · HintGuide · `TextureRect` và **`BoardSlot`** (Control `mouse_filter = 2`) — nơi
#        dành sẵn cho bàn cờ; anchors/offset của slot = đúng khung bàn cờ cũ (dọc 1020×1020 tại
#        (41,420) · ngang anchors 0.03/0.055/0.53/0.94). Đã xoá toàn bộ ext_resource controller.
#     3. Script bố cục `scripts/orientation/{portrait,landscape}/game.gd` thêm `board_slot()`.
#     4. `scripts/scenes/game.gd`:
#        · `_bind_refs()` — controllers/board tra 1 lần ở scene gốc (`get_node_or_null("Controllers/…")`,
#          `get_node_or_null("Board")`); UI thì tra theo bố cục đang hiển thị (ui/ui_child).
#        · `_mount_board()` — chuyển Board vào `BoardSlot` của bố cục đang active + `PRESET_FULL_RECT`.
#        · `_rebind_after_orientation()` gọn lại: chỉ `_bind_refs()` + gắn lại nút + `_apply_hud_for_mode()`;
#          **XOÁ** `_stop_quietly()` / `_restore_run()` (và biến `_difficulty`) — không còn ván thứ hai
#          để dựng lại. => XOAY MÀN HÌNH KHÔNG MẤT VÁN: mê cung, đường đã vẽ, đồng hồ, vị trí con trỏ
#          giữ nguyên (trước đây xoay là trắng bàn cờ và Replay nhảy sang Dungeon).
#        · `_ready()` dùng `ui("Status")` / `ui("Information")` (2 node này nằm trong bố cục).
#     - KIỂM CHỨNG `scripts/test_case/dev_probe_rotate_run.gd`: ngang kéo được `path=[(0,1),(1,1)]`
#       → xoay dọc vẫn `path=[(0,1),(1,1)]` + `mode=play`, board.parent=BoardSlot, rect dọc
#       (41,420) 1020×1020 · ngang (102.39,105.6) 1706.5×1699.2 (khớp thiết kế).
#     - GHI CHÚ: Board nằm NGOÀI layout nhưng khi chạy được chuyển VÀO `BoardSlot` để z-order
#       đúng (bàn cờ dưới Status/HUD). Đừng "tối ưu" bằng cách để Board là con trực tiếp của scene
#       — sẽ vẽ ĐÈ lên thanh trạng thái ở bố cục dọc.
# [x] VÒNG 13b — FIX "xoay sang DỌC thì lưới bàn cờ nằm sai chỗ, phải kéo cửa sổ mới về đúng":
#     · NGUYÊN NHÂN: bố cục BÊN TRONG bàn cờ (ô · tường · con trỏ) tính theo khung GIẤY của `Panel`,
#       nhưng khi Board được ĐỔI CHỖ, `NOTIFICATION_RESIZED` của bàn cờ chạy khi Panel còn cỡ CŨ
#       (hàng dọc 1646 của bố cục ngang) ⇒ lưới bị căn theo số cũ rồi KHÔNG tính lại (resize mới fix).
#     · SỬA 2 LỚP: `board.gd` nối thêm `Panel.resized → _update_layout_positions()` + thêm API
#       `relayout()`; bố cục gọi `board.call_deferred("relayout")` sau khi gắn (frame kế tiếp, mọi
#       kích thước đã ổn định). Kiểm chứng: probe in "ô đầu tiên" = (375.0, 738.06) ở bố cục dọc
#       (đúng tâm giấy 988×988) — trước khi sửa là (135.0, 1067.05).
#     · PROBE: `scripts/test_case/dev_probe_rotate_shot.gd` (ngang → xoay dọc → in số đo + chụp ảnh
#       vào `tmp_aspect/rotate/`).
# [x] VÒNG 13c — DỒN TOÀN BỘ KHÁC BIỆT DỌC/NGANG VÀO 2 SCRIPT BỐ CỤC (`game.gd` hết `if is_landscape`):
#     API chung (mỗi hướng tự cài trong `scripts/orientation/<hướng>/game.gd`):
#       · `is_landscape_layout()` · `hud_variant(P, L)` — chọn scene HUD của hướng mình
#       · `board_slot()` · `mount_board(board)` — gắn bàn cờ dùng chung + relayout
#       · `fit_hud(hud)` — dọc giữ 980, ngang co theo bề rộng sidebar
#       · `configure_tool_path_button(btn, hide_wall)` — dọc ghim nút VẼ ĐƯỜNG, ngang để action_bar lo
#     `scripts/scenes/game.gd` gọi qua `_layout_call(...)` / `_layout_is_landscape()` / `_hud_variant(...)`
#     ⇒ màn chơi không còn biết gì về dọc-ngang (thêm hướng mới chỉ cần 1 script bố cục).

# ----------------------------------------------------------------------------
# VÒNG SỬA 12 (2026-09-19): HỖ TRỢ MỌI TỈ LỆ MÀN HÌNH (PORTRAIT + LANDSCAPE)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user (kiểm thử trên Windows trước, emulator tablet sau):
#     1. CƠ CHẾ 1 CHỖ — mọi màn tự hưởng: `scripts/scenes/base.gd` (root của base.tscn,
#        12/13 màn instance base). Giữ `canvas_items` + `aspect = expand`; root tự co thành
#        CỘT NỘI DUNG 1080px CANH GIỮA: `size = (1080, canvas_h)`, `x = (canvas_w − 1080)/2`.
#        - Màn dọc cao (9:18→9:21): giãn dọc như trước (phần tử neo đáy bám đáy).
#        - Màn rộng hơn 9:16 (3:4 · 4:3 · 16:9 · 18:9… kể cả NGANG): nội dung gọn trong cột
#          giữa — không kéo giãn ngang, không lệch vị trí.
#        - `Background` (index 0 — dưới mọi nội dung) giữ đúng cột + thêm 2 dải `SideL`/`SideR`
#          (con của Background) tô màu giấy #FAF5EB phủ 2 bên → trang vở trải rộng, KHÔNG letterbox.
#        - Logic ở `_enter_tree()` + `_notification(READY/RESIZED)` + `viewport.size_changed`
#          (mọi màn con override `_ready()` không gọi super nên `_ready` của BaseScene không chạy).
#        - GOTCHA đã dính & sửa: đừng thêm node nền MỚI vào base.tscn — scene con chèn node ở
#          index 1..N nên node nền mới sẽ bị đẩy LÊN TRÊN nội dung (đã bị: PaperFill che hết UI);
#          phải nhét dải nền vào TRONG node Background (index 0) để giữ z-order.
#     2. POPUP: `scripts/nodes/popups/base.gd` — thẻ popup canh giữa theo cột; `Dim` phủ
#        TOÀN màn hình (kể cả 2 bên cột); tính lại khi mở popup/đổi cỡ.
#     3. ANDROID: `window/handheld/orientation = 6` (Sensor — xoay tự do 2 chiều).
#     4. TIME ATTACK (theo bản user tự chỉnh trong editor): thẻ THỜI GIAN dùng anchors tỉ lệ
#        (0.352..0.648 × 0.085..0.911 ≈ 291×206, canh giữa khung) + số `resize_font_to_fit`;
#        chỉnh lại về ĐÚNG tâm khung (user để lệch 20px phải); mockup matchup_time_attack.svg
#        sinh lại theo (helper card_time_tall tham số hoá w/h).
#     - FIX BUG THẬT (lộ ra khi quét): `nodes/daily/calendar.tscn` hàng thứ DayTitle
#       offset_right = 1366 (lệch 380px so với lưới Days 986) → nhãn T2..CN lệch cột ngày,
#       nhãn CN tràn màn hình ở MỌI tỉ lệ. Sửa về 986 (khớp 7 cột × 130px).
#     5. CÔNG CỤ KIỂM THỬ: `scripts/test_case/dev_aspects.gd` (chạy opengl3, không headless):
#        quét 10 tỉ lệ (9:16 · 9:18 · 9:19.5 · 9:21 · 3:4×2 · 4:3 · 16:9 · 18:9 · 19.5:9)
#        × (12 màn + 9 chế độ game + popup pause) = 220 lượt: cột canh giữa · nền giấy phủ kín ·
#        không node tràn màn hình · board/HUD/thanh nút trong cột · popup Dim phủ canvas.
#        Option: `--shots=1` (PNG nửa phân giải vào tmp_aspect/), `--only=`, `--sizes=`, `--modes=`.
#        KẾT QUẢ: **220/220 PASS**; render soi mắt các tỉ lệ tiêu biểu OK.
#     - Full suite 29/29 PASS (test_hud_modes 100 → 101 check theo thẻ Time mới).
#     - Docs: Number_Maze_Game_Design.md §13.10 (mới) + §5.3/§10.2b cập nhật.
#     6. CHẠY THẬT TRÊN MÁY ẢO ANDROID (tablet Medium_Tablet_API_28, 2560×1600 dpi320):
#        - Build: Godot 4.8 custom — `--headless --path . --export-debug "Android"
#          build/inkmaze_tablet_debug.apk` (~422MB, ~2 phút; lỗi cuối "EditorSettings not
#          instantiated yet ... shutdown_adb_on_exit" là vô hại, exit 0).
#        - LỆNH CHẠY MÁY ẢO ĐÚNG (quan trọng):
#          emulator -avd Medium_Tablet_API_28 -no-snapshot-load -no-snapshot-save -no-boot-anim
#                   -gpu host -feature GLESDynamicVersion -memory 4096
#          · `-gpu swiftshader_indirect` + `-feature GLESDynamicVersion` → app vào được nhưng
#            **SIGSEGV ngay trong GLThread** (crash render SwiftShader, màn hình xám).
#          · `-gpu host` (dùng thẳng NVIDIA RTX 3060 — GLES 3.0/4.5 driver + Vulkan 1.3) →
#            chạy ổn định, KHÔNG crash, render nét.
#          · Thiếu `-feature GLESDynamicVersion` với swiftshader → chỉ có ES 2.0 → Godot
#            `eglCreateContext EGL_BAD_CONFIG: no ES 3 support` → crash.
#        - Ảnh chứng cứ (giữ ngoài project: `d:\godot-phone-game\inkmaze_shots_android\`):
#          splash · main · game (Play chế độ MÀN 02) · popup Tạm dừng (Dim phủ kín) ·
#          Cài đặt — mỗi màn chụp ở CẢ landscape 2560×1600 lẫn portrait 1600×2560 → cột 1080
#          canh giữa + 2 dải giấy #FAF5EB hai bên đúng như thiết kế, xoay máy reflow ngay.
#        - Ghi chú: `adb shell settings put secure immersive_mode_confirmations confirmed`
#          trước khi chạy để bỏ overlay "Viewing full screen / GOT IT" của Android.
#        - FIX nhỏ kèm theo: `base.gd` `_enter_tree()` gọi `set_anchors_preset(PRESET_TOP_LEFT, true)`
#          để hết cảnh báo "non-equal opposite anchors" (từ đó Root tự quản size/position).
#     7. FIX LỖI TRÊN MÁY ẢO (user báo): đâm tường không thấy popup Game Over · popup
#        Dungeon hiển thị sai · màn loading lỗi.
#        NGUYÊN NHÂN: app giữ cửa sổ NGANG (2560×1600, canvas 3072×1920) trong khi MÀN HÌNH
#        đã xoay DỌC (1600×2560) — do xoay bằng khoá xoay/adb (cảm biến không đổi) hoặc mở app
#        lúc máy đang xoay. Hệ quả: mép phải bị cắt, popup canh giữa canvas 3072 nằm ngoài vùng
#        thấy (tưởng "không hiện") , lớp Dim vẫn chặn input, màn loading vẽ sai vùng.
#        SỬA: `AppManager.sync_window_orientation()` — watchdog 0,5s, phát hiện window khác hướng
#        screen thì `window_set_size(screen)` (chỉ trên mobile).
#        + `PopupManager.get_host()` không tái dùng host đã cache khi host không còn thuộc scene
#        hiện tại (tránh popup nằm trong scene cũ đã bị xoá ⇒ không thấy popup).
#        Kiểm chứng: máy ảo tablet — mở app lúc màn hình dọc ⇒ tự về dọc đúng ngay, main/game/
#        levels/popup Tạm dừng hiển thị đủ; full suite 29/29 PASS; harness tỉ lệ 220/220 PASS.

# ----------------------------------------------------------------------------
# VÒNG SỬA 11 (2026-09-19): TIME ATTACK HUD GIỮA · SUM PATH REPLAY · COUNTDOWN KHOÁ · NÚT X POPUP
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user (4 điểm):
#     1. Time Attack — HUD chỉ còn THỜI GIAN đặt giữa (đổi cả mockup):
#        - Scene mới `nodes/hud/time_attack_hud.tscn` (`TimeAttackHUD` extends BaseHUD):
#          thẻ `card_time_tall.svg` 250×156 tại (365,46) = GIỮA khung 980×249 (giữa cả 2 chiều),
#          dòng phụ `STR_HUD_TIME_COUNTDOWN` ("Đếm ngược"); KHÔNG còn thẻ THỬ THÁCH
#          (`challenge_card()` = null — thử thách vẫn chấm và hiện ở popup kết quả).
#        - `GameScene`: thêm `HUD_TIME_ATTACK` + map `_hud_scene_for/_hud_class_for("time_attack")`.
#        - Mockup `mockup/matchup_time_attack.svg` sinh lại bằng `tools/mockup/gen_matchup.py`
#          (thêm helper `card_time_tall` + spec mới; tool vẫn bỏ qua 5 mockup vẽ tay).
#     2. Sum Path — hết đường thắng thì hiện nút CHƠI LẠI dưới Vẽ Đường/Ghi Nhớ:
#        - `SumPathGameMode.is_unwinnable()` = (operator "<" hoặc "=") and current_sum > target_val;
#          thêm `on_move_undone()` + `_recompute_sum()` (undo lùi bước -> tổng tính lại theo đường).
#        - Nút `Replay` (TextureButton 192×108: art `common/btn_paper_secondary_*` + icon_replay.svg
#          + nhãn `STR_BTN_REPLAY`) ẩn sẵn trong `scenes/game.tscn` tại (444,1700) — DƯỚI thanh nút;
#          `pressed` -> `Controllers/GameController.restart_run` (nối trong .tscn).
#        - `UIController`: `@export replay_button` + `set_replay_visible()/replay_shown()`
#          (pop-in khi hiện); `GameController._update_hud()` tính cờ `replay_visible`.
#     3. Countdown Cost — hết ngân sách: KHOÁ di chuyển + NHẤN MẠNH nút Undo:
#        - `GameController._update_hud()`: mode countdown + `steps_remaining <= 0` ->
#          `grid_view.set_interaction_enabled(false)` + cờ `undo_highlight`; chỉ MỞ LẠI khi VỪA
#          hết khoá (`_countdown_locked`) — KHÔNG bật tuỳ tiện kẻo ghi đè khoá của hệ thống khác
#          (pha GHI NHỚ của Blind Memory — đã dính lỗi này và sửa trong cùng vòng).
#        - `UIController`: `@export undo_button` + `set_undo_highlight()` (ám vàng + `UIAnim.play_pulse`).
#        - Undo hoàn ĐÚNG chi phí bước vừa đi: `GameController._move_costs` (append mỗi bước đi được,
#          pop khi undo) — trước đây refund cứng 1 bước (sai với ô giá 2..4, hết ngân sách sẽ kẹt).
#     4. Popup Instruction — bấm nút X (Close) để đóng: `Close` là **TextureButton** (kế thừa
#        BaseButton, KHÔNG phải Button) nên cast cũ `as Button` trả null -> nút X chưa bao giờ được
#        nối signal; sửa `close_button() -> BaseButton`. test_instruction +9 check (bấm X đóng mỗi chế độ).
#     - Test cập nhật: test_hud_modes 72 -> 100 check (4b Time Attack HUD · 6 Sum Path replay ·
#       7 Countdown khoá + undo hoàn đúng chi phí) · test_instruction 285 -> 294 check.
#       Full suite 29/29 PASS; render SubViewport xác nhận bằng mắt (Time giữa khung · CHƠI LẠI
#       dưới thanh nút · Undo ám vàng khi hết ngân sách).

# ----------------------------------------------------------------------------
# VÒNG SỬA 10 (2026-09-19): LỚP MỰC PHAI TRÊN Ô · CON TRỎ ×3 · VUỐT RANKING · SHOP VỪA KHUNG
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user (4 điểm):
#     1. Fading Ink — lớp hiển thị theo mực còn lại (theo mockup matchup_fading_ink):
#        ô mực = 1 hiện nền hổ phách + chữ "SẮP PHAI" (asset mới cell_warn_wash.svg +
#        text_game_cell_warn.tres), ô mực = 0 hiện lớp gạch + huy hiệu "CẠN"
#        (cell_faded_overlay.svg + cell_depleted_badge.svg + text_game_cell_faded.tres).
#        - cell.tscn thêm 5 node Warn/WarnLabel/Faded/FadedBadge/FadedLabel nằm TRƯỚC
#          Label số (số luôn vẽ trên cùng); bỏ asset không dùng cell_warn_chip.svg.
#        - cell.gd: set_ink_left(ink) (1=warn · 0=faded · khác=ẩn hết) + warn_visible()/
#          faded_visible()/warn_text()/faded_text(); chữ phụ co theo set_font_size()
#          (tỉ lệ 11/56 và 22/56, sàn 8/10px).
#        - board.gd refresh_cell_texts(): mode có ink_left() -> set_ink_left() thay vì mờ
#          cả ô bằng modulate alpha 0.4 (để huy hiệu CẠN không bị mờ); ô S/F không gắn lớp.
#     2. Con trỏ người chơi TO GẤP 3: player_cursor.tscn 44×44 -> 132×132;
#        FALLBACK_CURSOR_SIZE 44 -> 132 trong board.gd (co theo _fit_scale, sàn 18px).
#     3. Xếp hạng: hàng RankRow (Control STOP) "ăn" sự kiện kéo nên ScrollContainer không
#        cuộn được -> ranking.gd tự xử lý _input (ngưỡng 14px · trục dọc
#        scroll_vertical = _drag_scroll − delta.y · set_input_as_handled · reset 0 khi đổi tab).
#     4. Cửa hàng VỪA khung nhìn (không cần vuốt dọc): thẻ ô 475×315 -> 475×294 + xếp lại
#        nội bộ (item_tile.tscn) · List separation 30 -> 20 (shop.tscn) · khe dọc lưới
#        30 -> 24 (shop.gd _make_grid). Kiểm: pen 215+20+930=1165 · theme 950 · tool 1180
#        · coin 980 — đều ≤ khung 1200.
#     - Test cập nhật: test_fading_ink (3 trạng thái ô + ô S/F) · test_board_fit (chữ phụ co)
#       · test_ranking (+4 check vuốt) · test_shop (tile 294 + mọi tab vừa khung nhìn).
#       Full suite 29/29 PASS; render kiểm tra bằng mắt (fading ink warn/faded, shop
#       pen/tool/coin, ranking cuộn 320px).

# ----------------------------------------------------------------------------
# VÒNG SỬA 9 (2026-09-18): POPUP HƯỚNG DẪN RIÊNG CHO TỪNG CHẾ ĐỘ (9 SCENE)
# ----------------------------------------------------------------------------
# [x] LÀM theo yêu cầu user: bỏ popup hướng dẫn dùng chung, làm 9 popup riêng
#     theo bộ mockup mới `mockup/instruction/*.svg` + ảnh minh hoạ đã tách sẵn
#     trong `assets/images/instructions/guideline_image/`.
#     - Scene riêng: `nodes/popups/instruction/<mode>.tscn` (normal_maze ·
#       dungeon · minesweeper · sumpath · countdowncost · fadingink ·
#       blindmemory · fog_of_war · time_attack) — instance `base.tscn`,
#       nội dung 3 trang nướng thẳng vào scene theo đúng mockup.
#     - Script hành vi chung `scripts/nodes/popups/instruction_popup.gd`
#       (class InstructionPopup): vuốt ngang · lăn chuột · phím ←→ · tab ·
#       dots · Prev/Next (khoá ở trang 1 / trang 3) · CTA (trang cuối = đóng) ·
#       link (trang cuối = về trang 1, trang trước = bỏ qua) · sfx.
#     - Ảnh minh hoạ xử lý ThorVG-safe bằng `tools/mockup/prepare_guideline_images.py`:
#       nhúng `<use>` (cầu thang) + chuyển `<text>` (số grid, ✓, ➔→•) thành path
#       glyph thật (Be Vietnam Pro Black + fallback Noto Sans JP). Ảnh gốc backup
#       tại `mockup/instruction/_extracted_source/`.
#     - Chữ 100% qua khoá `STR_GI_*` (573 câu vi/en + token) trong
#       `resources/localization/string_extra.csv`; bảng dịch EN:
#       `tools/content/guide_text_en.py`; sinh key: `tools/content/build_instruction_data.py`;
#       generator scene: `tools/mockup/gen_instruction_popups.py` (giữ nguyên
#       uid + unique_id của 8 scene placeholder cũ).
#     - Nối dây: `GameController.INSTRUCTION_SCENES` → `Popups.open_path`;
#       gỡ id "instruction" khỏi `PopupManager.POPUPS`; xoá file cũ
#       `nodes/popups/instruction.tscn`, `scripts/nodes/popups/instruction.gd`,
#       `scripts/utils/instruction.gd`.
#     - Test: `scripts/test_case/test_instruction.gd` — 186 check (9 scene ·
#       tab/nav/CTA/link · đủ bản dịch vi+en · tích hợp nút "?" mở đúng scene).
#       Full suite 29/29 PASS; render kiểm tra bằng mắt (dungeon, time_attack,
#       minesweeper, fog of war).
# [x] TINH CHỈNH theo yêu cầu user (cùng ngày):
#     - Số/ký hiệu trên grid (1 · 2 · 15 · 04 · 01:24 · = · ? · S · F…) ghi
#       TRỰC TIẾP vào scene — bỏ 25 khoá dịch token (`is_literal_text()` trong
#       tools/mockup/instruction_mockup.py); chỉ chữ có nghĩa dùng STR_GI_*.
#     - Tab · nav ‹ › · dots · nhãn TRANG dùng chung 1 bộ NGOÀI Pages (không lặp
#       lại mỗi trang); script đổi trạng thái qua `@export` StyleBoxFlat/màu
#       nướng sẵn trong scene (tab chọn = accent, tab thường = xám; dot chọn =
#       viên thuốc accent, dot thường = chấm `#D1E2ED`); nav giữ 2 bộ style
#       thường/khoá — khoá sẵn màu mờ xanh (prev) / mờ xám (next) như mockup.
#     - CTA · Link VẪN nằm trong từng trang vì mockup vẽ khác nhau mỗi trang.
#     - test_instruction.gd 285 check (thêm: chrome dùng chung không lặp trong
#       Page1 · số ghi trực tiếp · bấm dots/Prev/Next). Full suite 29/29 PASS.

# ----------------------------------------------------------------------------
# VÒNG SỬA 8 (2026-09-15): CHẶN NHẢY CHƯƠNG + POPUP CHỌN CHƯƠNG + BANNER FOCUS
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user (4 điểm):
#     1. Xong màn cuối chương KHÔNG tự nhảy sang màn của chương chưa unlock:
#        `record_level_clear()` chỉ mở màn kế khi màn đó TỒN TẠI và CHƯƠNG CỦA NÓ ĐÃ MỞ;
#        thêm `next_level_in_chapter()` (màn kế trong CÙNG chương, -1 nếu hết chương),
#        `can_play_level()`, `has_unlockable_chapter()`. GameController đi tiếp bằng
#        `next_level_in_chapter` + `can_play_level` (không còn `current_level + 1`).
#     2. Popup thắng màn: hết chương (hoặc chương kế chưa mở) -> nút phải đổi từ
#        "MÀN KẾ TIẾP" thành "CHỌN CHƯƠNG", bấm là ra màn Chọn Chương (cờ `next_available`
#        do `_complete_floor()` tính, `winning.gd` đổi nhãn).
#     3. Banner chương ở màn Chọn màn có trạng thái FOCUS khi có chương đủ Sao để mở:
#        art mới `level_selector/chapter_banner_focus.svg` (viền nét đứt hổ phách) + nhấp nháy
#        + dòng gợi ý `STR_CHAPTER_UNLOCKABLE` (variation `LevelsChangeChapterFocus`).
#     4. Sửa ô đếm Sao sai ("30/27"): giờ hiện Sao CỦA CHƯƠNG đang xem
#        (`chapter_stars` / `chapter_star_total`); nút chân trang theo màn chưa đạt sao đầu tiên
#        của chương và đổi thành "CHỌN CHƯƠNG" khi xong hết chương; mở màn đúng trang chứa màn đó.
#     - Test: `test_chapters.gd` 243 check (thêm: gate theo chương, next_level_in_chapter,
#       số sao theo chương, banner focus, nút popup đổi nhãn) · `test_levels_paging.gd` cập nhật
#       theo luật mở trang mới · 23/23 suite PASS.

# ----------------------------------------------------------------------------
# VÒNG SỬA 7 (2026-09-15): TINH CHỈNH MÀN CHỌN CHƯƠNG THEO PHẢN HỒI USER
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user (7 điểm):
#     1. Icon ổ khóa bị lệch: ổ khóa LỚN trên doodle đặt lại (122,91) để THÂN khóa
#        trùng tâm doodle; ổ khóa TRONG nút bị đè lên chữ ("CẦN 45" bị cắt) -> nút phóng to
#        250x100 + tách vùng icon (20,32 36x36) và vùng chữ (66..244) nên không còn chồng nhau.
#     2. Bấm banner chương ở màn Chọn màn: không cần bấm đúng chữ "ĐỔI CHƯƠNG" nữa —
#        nối `gui_input` cho cả node `ChapterBanner` (nhãn + VBox đặt mouse_filter = IGNORE).
#     3. Nút CHƠI ở Main vào thẳng màn CHỌN MÀN (như cũ), màn Chọn Chương thành màn con
#        (Back ở Chọn Chương -> Chọn màn; Back ở Chọn màn -> Main).
#     4. Mỗi chương 1 ICON riêng: `ChapterData.icon` + 4 SVG mới (icon_intro 3×3 L ·
#        icon_logic 5×5 zigzag · icon_trap 7×7 + 2 dấu X nét đứt · icon_master 9×9 xoắn + sao);
#        chưa đặt icon -> fallback theo cỡ bàn (maze_small/medium/large). Tool Python cũng
#        có ô chọn icon + tự gợi ý theo số chương.
#     5. Chỗ TRỐNG bên trái nút của chương đã mở/đang chơi -> đặt icon TAM GIÁC PLAY
#        (icon_star_white cho nút MỞ KHÓA để không bị chìm màu trên nền hổ phách).
#     6. Hết bị "cropping": nút 225x95 -> 250x100 (đủ chỗ icon + chữ), chip kích thước
#        125x28 -> 260x34, chip còn thiếu 145x26 -> 195x32, chip đã đủ 240x34 -> 300x40,
#        ruy băng 160x46 -> 190x48 (chữ không còn tràn ra ngoài art).
#     7. Chữ to hơn toàn bộ màn chương: tiêu đề 34->40 · mô tả 18->22 · sao 18->23 ·
#        chip 13->17 · nút 22->26 (+phụ 13->17) · ruy băng 14->17 · ví sao 34->40 ·
#        banner 20->24; banner màn Chọn màn 26->30 + dòng ĐỔI CHƯƠNG 22->24 · CTA 34->36.
#     - Test: `test_chapters.gd` 213 check (thêm: icon riêng từng chương, nút có icon play,
#       ổ khóa canh giữa, chữ >= ngưỡng, banner bấm được, luồng điều hướng mới).
#       Godot 23/23 suite PASS · tool 89 unittest + selftest PASS.
#     - GOTCHA .tscn: `layout_mode` phải đứng TRƯỚC các dòng `offset_*`, nếu không nhãn
#       (RibbonLabel) bị dời về sai chỗ và mất chữ.

# ----------------------------------------------------------------------------
# VÒNG SỬA 6 (2026-02): HỆ THỐNG CHƯƠNG (CHỌN CHƯƠNG + ICON CHƯƠNG + TOOL)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("tạo Scene Chapter Selection / Icon cho các chương
#     đang bị close / update tool để tạo chapter + add Level vào Chapter"):
#     - Dữ liệu: scripts/resources/chapter_data.gd (ChapterData) + 4 file
#       resources/chapters/chapter_1..4.tres (NHẬP MÔN 0 sao · SUY LUẬN 25 · BẪY ẨN 45 · BẬC THẦY 70).
#       Quan hệ NGƯỢC: LevelData.chapter -> chương (thêm màn vào chương = ghi `chapter` cho file màn).
#     - LevelManager: get_chapters/get_chapter/chapter_count/has_chapter · chapter_of_level ·
#       levels_in_chapter · chapter_star_total/chapter_cleared_count/chapter_stars · current_chapter_id.
#       (Sửa thêm: refresh_levels() giờ xoá cả cache LevelData — trước đây đổi thư mục level bị lẫn dữ liệu.)
#     - GameManager: unlocked_chapters + current_chapter (lưu/tải/xoá trong save) · total_stars() ·
#       can_unlock_chapter() · unlock_chapter() (signal chapter_unlocked + Save.queue_save) · go_to_chapters().
#     - 21 SVG mới assets/images/chapters/ (3 nền thẻ, halo, ribbon, 3 chip, thanh tiến độ, ví sao,
#       băng hướng dẫn, 3 nút play/unlock/locked, 3 doodle mê cung nhỏ/vừa/lớn, ổ khóa) + ~30 theme
#       variation Chapter* + ~20 khoá dịch STR_CHAPTER_* ở string_extra.csv.
#     - Scene mới: nodes/chapters/chapter_card.tscn (thẻ chương 5 trạng thái: ĐANG CHƠI · ĐÃ MỞ ·
#       ĐÃ ĐỦ ĐIỀU KIỆN (nút MỞ KHÓA + hào quang) · ĐANG KHÓA (ổ khóa + chip CÒN THIẾU n SAO) · SẮP RA MẮT)
#       + scenes/chapters.tscn & scripts/scenes/chapters.gd (ví Sao · băng hướng dẫn · danh sách cuộn · CTA).
#     - Màn Chọn màn (scripts/scenes/levels.gd) chỉ hiện màn của current_chapter + banner "CHƯƠNG n: Tên"
#       + dòng ĐỔI CHƯƠNG (bấm được) + Back về màn Chọn Chương; nút CHƠI ở Main mở màn Chọn Chương;
#       Nav/SceneManager có SCENE_CHAPTERS + goto_chapters; Debug Console có mục "Select Chapter".
#     - Tool Python (tools/level_designer): app/models/chapter.py + app/services/chapter_io.py +
#       app/models/chapter_repository.py + app/views/chapter_dialog.py; menu "Chương" (Ctrl+Shift+C) +
#       nút "Chương…" trên thanh công cụ; hộp thoại tạo/sửa/xoá chương, "Tạo chương theo dữ liệu màn",
#       gán màn vào chương (danh sách "1, 2, 3" hoặc "Gán MÀN ĐANG MỞ"), gợi ý nhãn kích thước; danh sách
#       màn hiện thêm cột chương.
#     - Test: scripts/test_case/test_chapters.gd (192 check) — TOÀN BỘ 23/23 suite PASS; tool: 88 unit test
#       + tests/test_chapters.py + phần CHƯƠNG trong `python main.py --selftest`; render 1080×1920 so mockup.
#     - Tài liệu: Number_Maze_Game_Design.md **mục 12** (luồng màn hình · dữ liệu · 5 trạng thái thẻ ·
#       quy tắc mở khóa bằng Sao · vị trí UI · asset/theme/chuỗi dịch · file liên quan · tool Python).

# [ ] CHƯA LÀM (gợi ý cho vòng sau):
#     - Thêm màn cho chương 3/4 (chương 3-4 hiện chưa có màn nào -> thẻ hiện "SẮP RA MẮT").
#     - Hiệu ứng mở khóa chương: animation hào quang -> tan biến + SFX riêng (hiện dùng Sfx.ACHIEVEMENT).
#     - Popup xác nhận "Dùng n Sao để mở chương?" trước khi mở khóa (hiện bấm là mở luôn).

# ----------------------------------------------------------------------------
# VÒNG SỬA 5 (2026-11): CHUẨN HOÁ DESIGN DOC + BỘ MOCKUP MATCHUP 9 CHẾ ĐỘ
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("update Design markdown theo design template + tạo mockup matchup cho các mode"):
#     - Number_Maze_Game_Design.md:
#        · Thêm **mục 0 — Tiêu chuẩn tài liệu (Design Template)**: bảng thông tin tài liệu (phiên bản, trạng thái,
#          "code là nguồn sự thật"), **template 9 trường cho mỗi bộ luật** (Cổng vào · Bàn cờ · Số trên ô ·
#          Di chuyển · Thắng · Thua · Hồi sinh · HUD · Chi tiết) và **quy ước màu/chất liệu UI** (giấy kem, lưới vở,
#          lề đỏ, thẻ giấy + dòng kẻ ngang, mực #224C6D, không emoji trong UI thật).
#        · Mục 5 viết lại theo template: 5.1 Play · 5.2 Dungeon (bảng spec + phần diễn giải cũ) ·
#          5.3 Time Attack · 5.4 Minesweeper · 5.5 Blind Memory · 5.6 Fog of War · 5.7 Sum Path ·
#          5.8 Countdown Cost (kèm bảng chi phí/ngân sách) · 5.9 Fading Ink — mỗi mục có trường **HUD** trỏ
#          tới đúng scene HUD + file mockup. Đã xoá các bullet trùng lặp cũ.
#        · Thêm **mục 10 — Mockup matchup & đặc tả HUD theo chế độ**: bảng 9 chế độ → mode_id → HUD scene →
#          mockup → thẻ; bảng **vị trí/kích thước thẻ** đo trực tiếp từ scene (khung 980×249 tại (50,175));
#          cách sinh lại mockup bằng tool.
#     - Bộ mockup mới (tools/mockup/gen_matchup.py sinh ra, KHÔNG ghi đè matchup_level/dungeon):
#        mockup/matchup_{time_attack,minesweeper,blind_memory,fog_of_war,sum_path,countdown_cost,fading_ink}.svg
#        · 1080×1920, đúng chất liệu (nền vở + lề đỏ + thẻ giấy + dòng kẻ ngang), vẽ ĐÚNG toạ độ thật:
#          khung HUD (50,175) 980×249 · Board (41,420)-(1061,1440) · thanh nút (73,1528)-(1031,1688) · Status (50,85).
#        · Mỗi mockup có **badge số + bảng chú thích** dưới thanh nút; minh hoạ riêng cho luật từng mode
#          (mìn đã nổ + icon Bomb, sương mù, ô hết mực mờ đi, popup đếm ngược Blind Memory, phép tính Sum Path...).
#        · Tool có 3 chế độ: mặc định (sinh mockup) · `--list` · `--dump` (in toạ độ node thật của các scene HUD).
#     - Đã render kiểm tra bằng browser (Chromium): nội dung text của các mockup khớp spec; 20/20 bộ test PASS.

# ----------------------------------------------------------------------------
# VÒNG SỬA 4 (2026-11): BỎ AREA MAZE -> MODE MỚI "FADING INK" (MỰC PHAI)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user (loại Area Maze, thay bằng bộ luật mới):
#     - Mode mới `scripts/modes/fading_ink_game_mode.gd` (class FadingInkGameMode, mode_id "fading_ink"):
#        · Bàn KHÔNG có tường trong (`MazeData.create_empty`) — số trên ô là MỰC của ô, KHÔNG phải số tường.
#        · CHỈ đi vào ô còn mực; ô hết mực bị CHẶN (allowed=false, không mất bước), mất số và MỜ đi (alpha 0.4).
#        · MỖI BƯỚC ĐI làm MỌI ô phai 1 điểm mực; mực hiện tại = mực_ban_đầu - số_bước_đã_đi (tính lại từ
#          `moves_made` nên Undo tự "hồi mực", không cần lưu lịch sử).
#        · Sinh bàn đảm bảo ĐƯỜNG NGẮN NHẤT luôn đủ mực tới F (ô trên đường được cấp mực theo số bước cần tới nó,
#          ô ngoài đường mực thấp 2..6 làm lối tắt dự phòng). `initial_steps` = số bước đường ngắn nhất + 4.
#        · `is_dead_end()`: hết lối đi mà chưa tới F -> GridController signal `dead_end` -> `GameController._on_dead_end()`
#          -> popup thua với tiêu đề riêng `STR_GAME_OVER_NO_PATH` ("HẾT ĐƯỜNG ĐI!" / "NO PATH LEFT!").
#     - XOÁ `scripts/modes/area_game_mode.gd`; thay "area" -> "fading_ink" ở `GameModeController.set_mode_by_name`,
#       `GameManager.DAILY_MODES`, `tools/level_designer/app/config.py` (MODE_IDS), chuỗi `STR_MODE_AREA` ->
#       `STR_MODE_FADING_INK` ("FADING INK" / "MỰC PHAI"); cập nhật `test_all_game_modes.gd` + `test_game_scene_integration.gd`.
#     - Hook mới ở `BaseGameMode`: `on_move_undone()` (GridController.undo_last_move gọi khi lùi 1 bước) + `is_dead_end()`;
#       `Board.refresh_cell_texts(dim_unwalkable)` để vẽ lại số + làm mờ ô theo mode (gọi từ mode mỗi bước).
#     - Popup thắng dùng chuỗi mới `STR_RESULT_SUBTITLE` (bỏ "(Classic Maze)" lỗi thời trong `STR_WIN_SUBTITLE`).
#     - Test mới `scripts/test_case/test_fading_ink.gd` (sinh bàn · luật đi + mực phai · đường ngắn nhất luôn tới được F
#       với cả 3 độ khó · undo hồi mực · dead end · tích hợp scene + popup thua). 20/20 bộ test PASS.
#     - Design.md mục 5 (bảng luật, chi tiết 5.3–5.9, xếp hạng, mục 9.1) đã cập nhật: bỏ Area Maze, thêm Fading Ink.

# ----------------------------------------------------------------------------
# VÒNG SỬA 3 (2026-11): ART HUD MỚI + FIX BLIND MEMORY + POPUP ĐẾM NGƯỢC
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo phản hồi user ("HUD Minesweeper/SumPath xấu", "Blind Memory lỗi"):
#     - Chất liệu HUD mới = mẩu giấy trắng trên vở kẻ ngang: viền màu + lề sổ tay + DÒNG KẺ NGANG
#       (#9FC0D6, opacity 0.5). Art mới: assets/images/game/{card_time_slip,card_bomb,card_sum}.svg
#       + vẽ lại card_op.svg. Bỏ hết `modulate` trên art thẻ (trước bị bẩn màu).
#     - Minesweeper: thẻ BOM viền đỏ (card_bomb) + sticker icon_bomb bên phải + số mực đỏ 72px canh trái.
#     - Sum Path: TỔNG = card_sum (viền xanh đậm) + thẻ TOÁN TỬ nằm giữa + MỤC TIÊU theo lề, ký hiệu `= / < / >`
#       dùng mực ĐỎ; MỤC TIÊU chỉ hiện con số (dạng `12 = 23`).
# [x] ĐÃ SỬA Blind Memory (bị crash "Invalid assignment ... on a base object of type 'Nil'"):
#     - NGUYÊN NHÂN GỐC: GridController.set_maze() gọi game_mode.on_grid_setup() TRƯỚC khi board.setup_maze()
#       dựng lại tường -> mọi thay đổi bị ghi đè, và label đếm ngược tự vẽ trong board bị setup_maze xoá
#       -> tween ghi vào label đã free = crash. FIX: on_grid_setup() chuyển sang GameController._start_floor()
#       gọi SAU grid_view.setup_maze() (Fog of War cũng được lợi).
#     - Bỏ hẳn label đếm ngược trong board.gd; thay bằng Board.reveal_all_walls() / hide_all_walls().
# [x] ĐÃ THÊM pha GHI NHỚ Blind Memory:
#     - BaseGameMode.memorize_countdown_seconds (>0 = có pha ghi nhớ; BlindMemory = 3/2 giây);
#       GameController._start_memorize_phase() dừng đồng hồ + hiện tường, _on_memorize_finished() ẩn tường
#       + chạy đồng hồ (signal `memorize_finished` của UIController nối trong scenes/game.tscn).
#     - Popup mới nodes/popups/memory_countdown.tscn (+ scripts/nodes/popups/memory_countdown.gd): mẩu giấy
#       (assets/images/popups/paper_note_memory.svg + washi_tape) đếm 3-2-1-GO!, KHÔNG nền mờ để vẫn thấy tường,
#       close_on_back = false. Đăng ký id `memory_countdown` trong PopupManager.POPUPS + Popups.MEMORIZE.
#     - HUD mới cho Blind Memory: nodes/hud/blind_memory_hud.tscn + scripts/nodes/hud/blind_memory_hud.gd
#       (class BlindMemoryHUD) — THỜI GIAN + thẻ GHI NHỚ (KHÔNG thẻ Thử thách); game.gd map "blind_memory".
#     - Chuỗi mới: STR_HUD_BLIND_TITLE/HINT · STR_MEMORIZE_TITLE/HINT.
#     - Test mới scripts/test_case/test_blind_memory.gd (HUD riêng, tường hiện hết + khoá tương tác + đồng hồ
#       dừng + popup mở, hết đếm ngược thì ẩn tường + chạy đồng hồ từ 0). 19/19 bộ test PASS.
#     - Đã render kiểm tra: HUD Minesweeper/Sum Path mới, popup GHI NHỚ + mê cung hiện tường, sau đếm ngược tường ẩn.

# ----------------------------------------------------------------------------
# VÒNG SỬA 2 (2026-11): ICON BOMB TO + ĐẠP MÌN THUA NGAY + PANEL TOÁN TỬ SUM PATH
# ----------------------------------------------------------------------------
# [x] ĐÃ SỬA theo phản hồi user:
#     - Ô mìn: đổi icon sang `assets/images/game/icon_bomb.svg` (đúng icon user muốn), phóng to
#       60% ô (anchors 0.2..0.8, giữ tỉ lệ) và đặt node Bomb TRƯỚC Label -> số trên ô vẽ ĐÈ LÊN
#       icon. Thêm viền màu giấy (#FEFDFA, outline 8px) cho số khi có Bomb để vẫn đọc rõ
#       (MazeCell._apply_bomb_text_outline; LabelSettings phải duplicate vì dùng chung).
#     - Đạp mìn giờ THUA NGAY: `MinesweeperPathGameMode.instant_game_over_on_hazard = true`
#       (grid_controller giữ nguyên vị trí + GameController._on_step_consumed -> _game_over()
#       mở popup thua). Hồi sinh = quay lại ô trước đó.
#     - HUD Sum Path: thêm thẻ TOÁN TỬ ở GIỮA thẻ TỔNG và thẻ MỤC TIÊU (dạng `12 = 23`):
#       asset mới `assets/images/game/card_op.svg` 112x112 + node Operator (anchors 0.6776/0.2751/
#       0.7918/0.7249, khai báo CUỐI để vẽ đè lên 2 thẻ) + `sum_path_hud.gd` đổ `mode.operator`;
#       thẻ MỤC TIÊU chỉ còn con số (`str(mode.target_val)`).
#     - Test `test_challenge_controller.gd`: kiểm panel TOÁN TỬ nằm giữa 2 thẻ + hiện đúng ký hiệu,
#       MỤC TIÊU không chứa toán tử; đạp mìn -> assert `gc._run_active == false` + `Popups.has_open()`.
#     - 18/18 bộ test PASS · render kiểm tra: số "1" đọc rõ trên icon bomb, HUD Sum Path có badge ">"/"<"/"=" ở giữa.
#     - Design.md mục 5 đã cập nhật (bảng chế độ, bố cục HUD, chi tiết Minesweeper & Sum Path).

# ----------------------------------------------------------------------------
# HUD TÁCH SCENE THEO CHẾ ĐỘ + SỬA LUẬT MINESWEEPER + HỒI SINH ENDLESS (2026-11)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM: user tách khung "Information" thành các HUD riêng cho từng chế độ và yêu cầu
#     GameScene/UIController/ChallengeController chạy được với kiến trúc mới.
#     - nodes/hud/base.tscn: root "Hud" (Control, preset 0, khung 980x249 tại (50,175)) + base.gd
#        (class_name BaseHUD: update_hud(ctx) · challenge_card() · set_time() · set_label_text() · format_time()).
#        Mọi HUD kế thừa scene này nên chỉ cần khai báo thẻ bên trong.
#     - HUD theo chế độ (đã đổi tên cho khớp nội dung — trước đây 2 file bị ngược tên nhau):
#        nodes/hud/level_mode.tscn + scripts/nodes/hud/level_hud.gd   -> THỬ THÁCH + THỜI GIAN (LevelHUD)
#        nodes/hud/dungeon_mode.tscn + dungeon_hud.gd                 -> SỐ BƯỚC + THỜI GIAN + TẦNG (DungeonHUD)
#        nodes/hud/minesweep_hud.tscn + minesweep_hud.gd              -> BOM CÒN LẠI (còn/tổng) + THỜI GIAN
#        nodes/hud/sum_path_hud.tscn + sum_path_hud.gd                -> TỔNG HIỆN TẠI + MỤC TIÊU (<,>,=) + THỜI GIAN
#     - scripts/scenes/game.gd: thêm HUD_* preload + hud_host + @export challenge_controller;
#        _hud_scene_for()/_hud_class_for()/_apply_hud_for_mode() (thay node, giữ index vẽ, slide-in)
#        + _bind_hud_nodes() gắn HUD mới cho ui_controller.set_hud() và challenge_controller.card;
#        switch_mode() gọi _apply_hud_for_mode() TRƯỚC start_new_run().
#     - scripts/core/controllers/ui_controller.gd: bỏ apply_mode_layout() + các export thẻ HUD cũ
#        (step_card/floor_card/challenge_card/step_val_label/time_val_label/floor_val_label) —
#        thay bằng hud: BaseHUD + set_hud(); update_hud(..., extra_info, mode) giờ đóng gói ctx
#        {title, subtitle, steps_remaining, elapsed_time, floor_number, extra, mode} cho HUD tự vẽ.
#     - scenes/game.tscn: khung Information về đúng (50,175)-(1030,424) + nối challenge_controller.
#     - Chuỗi mới: STR_HUD_BOMB ("BOMBS LEFT"/"BOMB CÒN LẠI") · STR_HUD_SUM ("CURRENT SUM"/"TỔNG HIỆN TẠI")
#        · STR_HUD_TARGET ("TARGET"/"MỤC TIÊU") trong resources/localization/string_extra.csv.
# [x] ĐÃ SỬA: Endless (Dungeon) hồi sinh TẠO LẠI màn — nay giữ nguyên mê cung + vị trí + đường đã vẽ
#     + tường đã lộ, chỉ cộng `revive_bonus_steps`, chạy tiếp đồng hồ và bật lại tương tác
#     (GameController.revive_run: bỏ _start_floor(), thêm timer_controller.resume() + set_interaction_enabled(true)).
# [x] ĐÃ SỬA Minesweeper (user báo 3 lỗi):
#     - Đạp mìn bị đưa về S -> thêm cờ `BaseGameMode.respawn_on_hazard` (mặc định true, Dungeon giữ nguyên);
#        MinesweeperPathGameMode đặt false -> grid_controller chỉ teleport về S khi cờ này bật.
#     - Mìn nổ LẶP LẠI mỗi lần đi vào -> evaluate_move chỉ hazard khi mìn CHƯA lộ; đi lại vào ô đã nổ hợp lệ.
#     - Ô hiện chữ "X" ghi đè con số -> get_cell_text() bỏ nhánh "X", giữ số; thêm has_bomb_marker()/
#        get_mines_left(); nodes/game/cell.tscn thêm node Bomb (icon_bomb.svg, góc trên-phải, ẩn mặc định)
#        + MazeCell.set_bomb()/has_bomb(); board.gd _sync_bomb_marker() + show_mine_hit() đánh dấu Bomb.
#     - HUD Minesweeper: thay panel MISSION bằng thẻ BOM CÒN LẠI `còn/tổng` (get_mines_left()/get_total_mines()).
# [x] ĐÃ SỬA Debug Console bị giật: cập nhật dòng FPS/MEM/Obj theo nhịp 0.2s (thay vì mỗi frame, tránh
#     reshape font liên tục) + số căn độ rộng cố định; _build() bỏ row cũ ngay trong frame (remove_child
#     rồi queue_free) và giữ nguyên vị trí cuộn (Scroll.scroll_vertical + set_deferred).
# [x] ĐÃ CẬP NHẬT: Number_Maze_Game_Design.md (mục 5 - bố cục HUD theo chế độ + kiến trúc HUD tách scene,
#     luật Minesweeper, HUD Sum Path, hồi sinh Endless) · test_challenge_controller.gd (HUD theo chế độ,
#     mìn nổ 1 lần, hồi sinh Endless giữ mê cung) · 18/18 test suite PASS · đã render kiểm tra mắt 4 HUD
#     (tất cả nằm đúng khung (50,175) 980x249) + biểu tượng Bomb trên ô đã nổ.

# ----------------------------------------------------------------------------
# LOADING SCENE + CHUYỂN CẢNH DÙNG NODE KHAI BÁO SẴN (2026-09)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): trước đây SceneTransition tự tạo Control + toàn bộ node con bằng code
#     (`_build_ui()`) nên các node chỉ tồn tại sau khi gọi play_transition() -> user muốn chúng NẰM SẴN trong scene.
#     - scenes/loading.tscn viết lại: root "Loading" (CanvasLayer, layer=128, process_mode=3) + script transition,
#        cây node khai báo sẵn: TransitionRoot / InputBlocker / PaperPage {PaperBg, ShadowLeft, ShadowRight,
#        Watermark (icon.svg 320x320 giữa trang)} / InkCircleDrawer / FadeRect.
#        Bóng đổ 2 mép giấy là 2 GradientTexture2D khai báo inline trong scene (trước đây sinh bằng code).
#        Các node con đều neo theo cha (full-rect / bám mép) nên KHÔNG cần set size/vị trí lúc chạy.
#     - scripts/nodes/common/scene_transition.gd: bỏ _build_ui()/_create_gradient_texture(); thêm
#        _bind_nodes() + hằng NODE_* (đường dẫn node trong loading.tscn) + has_ui();
#        nối signal `draw` của InkCircleDrawer trong _ready(); _run_page_turn() chỉ còn ẩn/hiện + tween.
#        Nếu thiếu node (ai đó gọi SceneTransition.new()) -> push_error + chuyển cảnh tức thì (không crash).
#     - scripts/manager/SceneManager.gd: _init_transition() instantiate scenes/loading.tscn (preload LoadingScene)
#        thay vì SceneTransitionScript.new().
#     - Test: test_scene_transition.gd dùng loading.tscn; kiểm tra đủ 9 đường dẫn node, has_ui(),
#        số node con KHÔNG đổi sau mỗi hiệu ứng (chứng minh không sinh node lúc chạy) + nhánh thiếu node.
# [ ] CÓ THỂ LÀM: hiện trang giấy phủ kín màn hình khi đổi scene — có thể thêm spinner/nhãn "ĐANG TẢI…"
#     vào loading.tscn để dùng đúng vai trò màn hình chờ khi scene nặng.

# ----------------------------------------------------------------------------
# HUD MÀN CHƠI THEO MOCKUP MATCH-UP MỚI (2026-09)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): user cập nhật mockup/matchup_level.svg + matchup_dungeon.svg -> trích asset & dựng lại HUD.
#     - assets/images/game/ (KHÔNG dùng stroke cho viền: vẽ 2 hình fill lồng nhau để ThorVG không cắt nét ở mép viewBox):
#        card_time.svg 250x138 (thẻ THỜI GIAN, viền xanh 3px, lề xanh x=18)
#        card_floor.svg 250x138 (MỚI - thẻ TẦNG, cùng kiểu thẻ thời gian)
#        card_steps.svg 440x158 (thẻ SỐ BƯỚC ở giữa, viền ĐỎ dày 4px, bo 24)
#        card_challenge.svg 720x246 (thẻ THỬ THÁCH, viền đỏ 3px, bo 22 — ĐÃ PHÓNG TO theo yêu cầu:
#          đáy thẻ dừng đúng MÉP TRÊN vạch xanh của bàn cờ, y=424 ở khung 1080x1920)
#        chal_row_done.svg / chal_row_pending.svg 490x58 (nền dải thử thách đã đạt / chưa đạt)
#        chal_check_done.svg / chal_check_pending.svg 30x30 (ô tích đỏ / ô chờ tích nét đứt)
#        chal_badge_done.svg 96x32 (nhãn đỏ "✓ ĐẠT") · ĐÃ XÓA card_score.svg (HUD Dungeon không còn thẻ điểm)
#     - resources/settings/text/: + text_game_card_label(18) · text_game_card_value_steps(72) ·
#        text_game_challenge_count(62) · text_game_challenge_count_sub(36) · text_game_challenge_small(16) ·
#        text_game_challenge_row(22 - tăng từ 18 cho cân với thẻ cao 246px)
#        (text_game_information_label 24 và _title_label 32 thành không dùng nữa - giữ lại phòng khi cần)
#     - scenes/game.tscn: "Information" từ HBoxContainer -> Control định vị tuyệt đối tại (50,175) rộng 980, cao 249;
#        Play Mode = Time(0,13) + Challenge(265,3);  Dungeon = Time(0,13) + Step(270,0) + Floor(730,10) (đúng toạ độ mockup).
#        Thẻ THỬ THÁCH: cột tổng kết (Label/CountRow[Count+CountMax]/Note + Divider) + 3 dải Row1..3 {Bg, Check, Name,
#        Status, Badge+BadgeLabel} + nhãn dùng khoá dịch STR_HUD_* (auto_translate).
#        Header "Status": Pause · Title(VBox: Subtitle + LevelLabel) · Restart  -> đồng bộ 2 dòng với mockup.
#     - scripts/core/controllers/challenge_controller.gd::_refresh_hud(): vẽ theo cấu trúc mới - Count "x",
#        CountMax "/3 ✓", Note "n ĐÃ HOÀN THÀNH", mỗi dải đổi Bg/Check theo done, dải đã đạt ẩn Status + hiện Badge,
#        màn khai báo < 3 thử thách thì ẩn các dải thừa (const ROW_DONE/ROW_PENDING/CHECK_DONE/CHECK_PENDING thay bộ sao).
#     - scripts/core/controllers/ui_controller.gd: apply_mode_layout(endless) -> Step+Floor (endless) / Challenge;
#        update_hud(title, subtitle, steps_remaining, elapsed_time, floor_number) - thẻ SỐ BƯỚC chỉ hiện số còn lại
#        (bỏ "/max"), thẻ TẦNG hiện "%02d", subtitle rỗng thì tự ẩn.
#     - scripts/modes/base_game_mode.gd: get_hud_floor_title() giờ trả TÊN CHẾ ĐỘ cho mọi mode (Dungeon không còn
#        "TẦNG 01" trên tiêu đề - số tầng đã ở thẻ TẦNG) + hàm mới get_hud_subtitle() (mặc định rỗng).
#        scripts/modes/standard_game_mode.gd: override -> title "MÀN 09" (STR_LEVEL_TITLE_FORMAT) + subtitle
#        "PLAY MODE · CHƯƠNG 1" (STR_CHAPTER_FORMAT, chương lấy từ LevelData.chapter).
#     - scripts/scenes/game.gd: đổi @onready sang $Status/Title/LevelLabel + $Status/Title/Subtitle + $Information/*/Value.
#     - Chuỗi mới: STR_HUD_TIME/STEPS/FLOOR/CHALLENGE · STR_CHALLENGE_DONE_COUNT · STR_CHALLENGE_BADGE_DONE ·
#        STR_CHAPTER_FORMAT · STR_LEVEL_TITLE_FORMAT.
#     - Test: test_challenge_controller.gd cập nhật theo node mới (Bg/Check/Badge, CountRow/Count, thẻ TẦNG);
#        13/13 bộ test game PASS; render 1:1 (SubViewport 1080x1920) đã đối chiếu 2 mockup.
# [ ] CHƯA LÀM: mockup/archivement_scene.svg (MỚI) chưa dựng màn Thành tích; popup thắng màn (winning.tscn)
#     vẫn theo layout cũ (xem mục THỬ THÁCH bên dưới).
# [ ] CÓ THỂ LÀM: thẻ THỜI GIAN bên trái vẫn 250x138 (thấp hơn thẻ THỬ THÁCH 246) - nếu muốn 2 thẻ cao bằng nhau
#     thì phóng to card_time.svg + card_floor.svg rồi giãn nội dung (chưa được yêu cầu).

# ----------------------------------------------------------------------------
# THỬ THÁCH & SAO + POPUP THUA THEO CHẾ ĐỘ (2026-09)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): Sao KHÔNG còn tính theo thời gian còn lại -> 3 Thử thách / match-up,
#     hoàn thành 1 Thử thách = 1 Sao (tối đa 3). Chỉ Dungeon Mode có bộ đếm số bước còn lại.
#     - scripts/core/controllers/challenge_controller.gd (MỚI): 3 thử thách chuẩn suy ra từ dữ liệu màn
#        1. Không đâm vào tường vô hình · 2. Đi không quá N bước (N = max_steps thiết kế)
#        3. Về đích dưới T giây (T = N × 3, kẹp 30..240s)
#        API: setup_for_floor(N) · refresh(state, floor_elapsed, final) · rows() · stars() · all_done()
#        refresh() có cache theo (wall_hits, moves, giây) nên gọi mỗi frame vẫn rẻ.
#     - scenes/game.tscn: thêm thẻ HUD "THỬ THÁCH" (Information/Challenge, art mới
#        assets/images/game/card_challenge.svg, 624x125, 3 dòng Star/Name/Status + Count "x / 3"),
#        NodePath card của ChallengeController + challenge_controller của GameController.
#     - scripts/core/controllers/ui_controller.gd: apply_mode_layout(endless) -> Dungeon hiện
#        BƯỚC CÒN + ĐIỂM SỐ, các chế độ khác chỉ hiện THỬ THÁCH + THỜI GIAN (đúng mockup matchup_level.svg).
#     - scripts/core/controllers/game_controller.gd: chốt ngưỡng khi vào màn/tầng; CHỈ Dungeon thua vì
#        hết bước (các mode khác không giới hạn bước); stars() thay cho "3 if hits==0 else 2";
#        hồi sinh tách thành revive_run() (test gọi trực tiếp được khi AdsManager còn là stub).
#     - Hồi sinh theo chế độ (Design.md 5.10): Level = quay lại bước trước đó (undo, KHÔNG cộng bước);
#        Dungeon = nhận thêm +3 bước để đi tiếp.
#     - Popup thua tách 2 bản: nodes/popups/gameover.tscn (Dungeon: bước còn lại, revive +bước, thêm
#        label StampTitle/StampCount "HẾT BƯỚC · 0 / 20 BƯỚC") và nodes/popups/gameover_level.tscn (MỚI:
#        tiêu đề "ĐÂM VÀO TƯỜNG VÔ HÌNH!", bảng 3 thử thách + trạng thái ĐẠT/CHƯA ĐẠT, con dấu in số
#        thử thách đã hoàn thành, revive "Quay lại bước trước đó") + scripts/nodes/popups/gameover_level.gd
#        (class GameOverLevelPopup) + PopupManager/Popups.GAME_OVER_LEVEL = "game_over_level".
#     - Chuỗi mới trong resources/localization/string_extra.csv (STR_CHALLENGE_*, STR_GAME_OVER_LEVEL_*,
#        STR_REVIVE_DESC_STEPS/UNDO, STR_GAMEOVER_STAMP_*) + 2 theme variation PopupStampTextDanger/SubDanger.
#     - Test: scripts/test_case/test_challenge_controller.gd (HUD theo chế độ, ngưỡng 15 bước/45s,
#        tính Sao: đang chơi=1 · hoàn hảo=3 · đâm tường=2 · chậm&lâu=0, popup level + hồi sinh undo).
#        Đã render kiểm tra mắt: HUD Play (thẻ THỬ THÁCH 1/3 + THỜI GIAN) và 2 popup thua.
# [x] ĐÃ SỬA (2026-09): Hồi sinh ở Level Mode bị ĐỨNG YÊN + không hiện tường vừa đâm
#     - Nguyên nhân 1: AdsManager (stub) trả về false -> GameController thoát sớm, nhưng popup vẫn tự
#       close() khi bấm HỒI SINH -> ván ở trạng thái thua (interaction off, timer stop, _run_active=false)
#       => người chơi không kéo được nữa. Sửa: popup KHÔNG tự đóng nữa (GameController đóng khi được
#       thưởng) + AdsManager thêm cờ `enabled = true` (giả lập xem xong quảng cáo) để chạy được khi chưa gắn SDK.
#     - Nguyên nhân 2: GridController đưa nhân vật về S khi đâm tường kể cả ở chế độ thua-ngay -> mất vệt
#       đường, hồi sinh không còn biết vị trí cũ. Sửa: chỉ chế độ Endless mới teleport về S.
#     - Bổ sung: GridController nhớ cạnh vừa đâm (last_hazard_from/to/type) + reveal_last_hazard_wall();
#       GameController.revive_run() gọi lại -> sau hồi sinh đoạn tường vừa đâm nháy đỏ rồi vẫn hiện (mực đậm).
#       Hồi sinh còn hoàn lại 2 bước: 1 cho nước đi bị undo + 1 cho bước mất khi đâm tường.
#     - Test: test_challenge_controller.gd phần hồi sinh chạy ĐÚNG luồng người chơi (đâm tường -> bấm
#       HỒI SINH trên popup) và kiểm tra: về ô trước đó, tường vừa đâm hiện, bàn cờ nhận lại input, đồng hồ
#       chạy lại, ván tiếp tục và ĐI TIẾP ĐƯỢC; kèm check Dungeon vẫn đưa về S khi đâm tường.
# ----------------------------------------------------------------------------
# HỆ THỐNG THỬ THÁCH (CHALLENGE) — 16 LOẠI, TỐI ĐA 3 / MÀN (2026-09)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): ngoài 3 thử thách mặc định, màn có thể chọn tối đa 3 trong 16 loại
#     - scripts/core/controllers/challenge_types.gd (MỚI, class ChallengeTypes): registry 16 loại
#        A. mặc định: no_wall · steps_max · time_max
#        B. ô/đường đi: only_numbered · avoid_numbered · no_revisit · visit_all · visit_all_numbered
#        C. độ dài (% số ô): len_min_percent · len_max_percent
#        D. tổng số trên ô đã đi: sum_lt · sum_le · sum_gt · sum_ge
#        E. công cụ: no_hint · no_undo
#       (màn không khai báo -> DEFAULTS = no_wall + steps_max(N=max_steps) + time_max(N×3, kẹp 30..240s))
#     - scripts/core/controllers/challenge_context.gd (MỚI): state + mode + maze + path + elapsed + final
#     - scripts/core/controllers/challenge_controller.gd: viết lại để chấm cả 16 loại
#        · metrics: ô đã đi (không trùng), số lần đi đè, tổng số trên ô, số ô có số đã đi / toàn board
#        · thử thách "vi phạm là mất ngay" (no_wall, only/avoid_numbered, no_revisit, no_hint, no_undo)
#          hiện trạng thái sống; loại khác hiện tiến độ (bước / giây còn lại / % ô / tổng) rồi chốt khi kết thúc
#        · cache theo (va chạm, bước, ô đã đi, đi đè, tổng, gợi ý, hoàn tác, giây, final) nên gọi mỗi frame vẫn rẻ
#     - scripts/resources/level_data.gd: + challenge_types: PackedStringArray · challenge_params: PackedInt32Array
#       + get_challenges() -> [{type, param}] (tối đa 3, bỏ loại lạ)
#     - scripts/core/game_state.gd: + hints_used / undos_used (reset mỗi tầng) cho thử thách "không gợi ý/hoàn tác"
#     - scripts/modes/base_game_mode.gd + standard_game_mode.gd: giữ current_level_data để lấy thử thách của màn
#     - scripts/core/controllers/game_controller.gd: dựng ChallengeContext mỗi lần cập nhật HUD, chốt khi thắng/thua,
#       đếm hints_used / undos_used trong hint() / undo()
#     - scenes/game.tscn: thẻ THỬ THÁCH dùng font 18 + cột trạng thái rộng hơn (nhãn dài không bị cắt "...")
#     - resources/localization/string_extra.csv: + 26 chuỗi thử thách (nhãn + trạng thái sống)
#     - Test game: scripts/test_case/test_challenge_types.gd (16 loại + giới hạn 3 + đọc LevelData + mặc định)
#       + test_challenge_controller.gd đã cập nhật theo ChallengeContext
#     - TOOL (tools/level_designer):
#        · app/models/challenges.py (MỚI): registry Python khớp ChallengeTypes (id · nhãn · loại tham số · mô tả)
#        · LevelModel.challenges: list[(type, param)] + set_challenge/clear_challenge/clear_all_challenges/
#          default_challenges + nằm trong snapshot (undo được)
#        · tres_io: ghi/đọc `challenge_types = PackedStringArray(...)` + `challenge_params = PackedInt32Array(...)`
#          (rỗng = PackedStringArray(); file cũ thiếu 2 dòng vẫn đọc được; quá 3 thì cắt còn 3)
#        · validator: chặn lỗi mâu thuẫn (only+avoid numbered), steps_max < đường ngắn nhất,
#          len_max_percent < % đường ngắn nhất, visit_all khi có ô không tới được, sum_gt/ge vượt tổng tối đa,
#          tham số ngoài khoảng; cảnh báo trùng loại / màn không có ô số; info khi < 3 thử thách (tối đa N Sao)
#        · inspector_view: mục "Thử thách (tối đa 3)" — 3 dòng combobox + spinbox tham số + nút × + 2 nút tiện ích
#        · editor_controller: set_challenge / set_challenge_param / clear_challenge / reset_challenges /
#          fill_default_challenges (đều có undo + dirty + sự kiện)
#        · tests/test_challenges.py (26 test mới, tổng 76) · make_samples.py: màn mẫu 10..14 giờ có bộ thử thách
#          (level_10: no_hint · level_11: 3 mặc định · level_12: no_revisit + len_max 80% ·
#           level_13: len_min 60% + no_undo · level_14: len_max 45%)
#        · README.md đã ghi format 2 trường mới; đã build lại dist/LevelDesigner.exe (--selftest exit code 0)
#     - ĐÃ KIỂM CHỨNG end-to-end: tool ghi level_10..14 -> Godot đọc đúng (no_wall(0), steps_max(9)...),
#       render HUD game hiện đúng nhãn/trạng thái của từng màn
# [ ] CHƯA LÀM: popup THẮNG màn (nodes/popups/winning.tscn) vẫn hiện "Số bước / Va chạm tường /
#     TỔNG ĐIỂM" theo bản cũ — cần đổi sang bảng các Thử thách + 3 sticker sao như mockup popup_win_level.svg
# [ ] CHƯA LÀM: màn chọn màn hiển thị chi tiết từng thử thách (mới chỉ có số Sao)


# ----------------------------------------------------------------------------
# LEVEL DESIGNER (PYTHON) — tools/level_designer
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): app thiết kế level riêng cho dev, kiến trúc MVC + Tkinter,
#     build thành 1 file .exe (không cần cài Python), ghi thẳng resources/levels/*.tres
#     - main.py              : giao diện + CLI (--selftest, --export-example, --level, --project)
#     - app/models/          : level.py (LevelModel) · repository.py (đọc/ghi/xoá file level)
#     - app/services/        : tres_io.py (parser .tres) · solver.py (BFS + phân tích) · validator.py (luật kiểm tra)
#     - app/controllers/     : events.py · editor_controller.py (công cụ, undo/redo) · app_controller.py (file ops)
#     - app/views/           : theme.py · grid_view.py · inspector_view.py · level_list_view.py · main_window.py
#     - tests/ (29 test) · build_exe.py/.bat · requirements*.txt · README.md · .gitignore (venv/, dist/, build/, out/)
#     => dist/LevelDesigner.exe (~11MB). Đã kiểm chứng: file .tres do tool ghi GIỐNG HỆT định dạng Godot
#        và load OK bằng Godot headless; 9 màn thật đọc/ghi lại không sai 1 byte dữ liệu.
# [ ] Level Designer: chưa sửa được custom_cell_values (giá trị ô đặc biệt của vài mode) — hiện giữ nguyên
# [ ] Level Designer: chưa có preview 1:1 theo khung 1080x1920 / xem trước bằng art thật của game
# [ ] Level Designer: chưa có sửa hàng loạt (đổi difficulty / max_steps / par_time cho nhiều màn cùng lúc)
#
# [x] ĐÃ SỬA (2026-09): Màn Chọn Màn — quá 9 màn thì không vuốt sang trang + dots sai + nút sai
#     - scripts/core/level_manager.gd: get_level_ids()/get_level_count() dò file level_*.tres thật
#       (dùng ResourceLoader.exists, chịu được id trống) + set_levels_dir()/refresh_levels() cho test
#     - scenes/levels.tscn: bỏ GridContainer 9 thẻ tĩnh -> CardArea/Scroll/Pages (ScrollContainer) + PaginationDots
#       (HBoxContainer) + nút CTA đúng mockup (thẻ giấy xanh 800x120 + icon play, anchor_bottom 0.9010417)
#     - scripts/scenes/levels.gd: dựng thẻ theo danh sách thật (9 thẻ/trang), TỰ xử lý gesture vuốt trong _input
#       (card là Button nên "ăn" sự kiện kéo -> ScrollContainer không nhận được), snap về trang gần nhất khi thả tay,
#       dots dựng động theo số trang + bấm dot để nhảy trang, mũi tên trái/phải + lăn chuột cho desktop,
#       mở sẵn trang chứa màn đang chơi, khoá bấm thẻ 0.15s sau khi vuốt
#     - level_card.gd: hiện số theo chương (1-1..2-3) thay vì luôn "1-x"
#     - Bỏ hardcode 9 màn: GameManager.record_level_clear() chỉ mở khoá nếu màn kế tồn tại;
#       GameController._on_continue_requested() dùng LevelManager.has_level()
#     - Asset: calendar/btn_play_daily_* -> common/btn_paper_cta_* (giữ uid, cập nhật daily.tscn);
#       thêm level_selector/{dot_active,dot_inactive,icon_play_triangle}.svg; xoá pagination_dots.svg (tĩnh)
#     - Test mới: scripts/test_case/test_levels_paging.gd (12 màn tạm trong user://test_levels)
# [ ] Màn Chọn Màn: nhãn "ĐỔI CHƯƠNG" còn tĩnh — cần bộ chọn chương khi có nhiều chương thật
#
# [x] ĐÃ LÀM (2026-09): BOARD DẠNG POLYOMINO — board không chỉ hình chữ nhật (H, thập tự, vòng, U...)
#     - Game data: scripts/resources/level_data.gd thêm `cell_mask` (w*h bytes, index = y*w + x,
#       1 = ô thuộc board, để trống = chữ nhật đầy đủ nên 9 màn cũ không đổi)
#     - scripts/core/maze_data.gd: set_cell_mask()/is_cell_active()/reset_cell_mask();
#       _apply_mask_walls() tự ép MỌI cạnh giáp ô trống thành tường hiện (như viền ngoài);
#       _compute_wall_counts() chỉ tính cạnh giữa 2 ô THUỘC BOARD (tổng quát hoá luật "không tính
#       tường viền" trước đó); has_wall() chặn mọi nước đi vào ô ngoài board; BFS cũng chặn theo
#     - Board render: scripts/nodes/game/board.gd — ô ngoài board KHÔNG có cell node
#       (mảng _cell_nodes dạng thưa + guard null), anchor chỉ tạo ở góc dính board,
#       tường chỉ vẽ ở cạnh dính board, _hit_cell bỏ qua ô trống, set_suspected_wall bỏ qua cạnh ngoài board
#     - Di chuyển: grid_controller.try_move_to() bỏ qua ô ngoài board (không hazard, không tốn bước)
#     - Tool: LevelModel.cell_mask + công cụ 6 (bật/tắt ô, kéo để vẽ), canvas tô ô trống + gạch chéo,
#       cạnh bao quanh board vẽ nét đậm, is_outline() (giữ is_border làm alias), nút
#       "Toàn bộ ô = board (chữ nhật)", số trên ô chỉ tính tường giữa 2 ô thuộc board
#     - KIỂM TRA ĐƯỜNG ĐI (theo yêu cầu): solver chỉ đi trong ô thuộc board; validator báo LỖI khi
#       S/F nằm ở ô trống hoặc KHÔNG có đường tới F (nút Lưu bị chặn), CẢNH BÁO khi có ô board
#       không tới được từ S; inspector hiện đường ngắn nhất + số ô board/ô trống
#     - Màn mẫu: resources/levels/level_10 (chữ H 4x3) · level_11 (thập tự 4x4) ·
#       level_12 (vòng 2 ô dày có lỗ 6x6) · level_13 (chữ U 2 ô dày 6x4) — chương 2
#       + script tạo lại: tools/level_designer/make_samples.py (--dry-run để chỉ kiểm tra)
#     - Test: scripts/test_case/test_polyomino_board.gd (mask, số tường, đường đi, cell node, chặn đi vào ô trống)
#       + tools/level_designer/tests/test_polyomino.py (tool: 50 test) — đã render kiểm tra mắt level 10/12#
# [x] ĐÃ SỬA (2026-09): board từ 6 ô trở lên bị TRÀN ra ngoài khung giấy
#     - scripts/nodes/game/board.gd::_compute_layout(): tính tỉ lệ vừa khít khung
#       _fit_scale = clamp(min(khung_w/số_cột, khung_h/số_hàng) / cỡ_ô_gốc, 0.24, 1.0);
#       _step = cỡ_ô_gốc × _fit_scale; _apply_metrics_scale() đặt lại size cell/anchor/cursor,
#       cỡ chữ số trên ô, và bề rộng tường/suspected/history/moving/drag line (có ngưỡng nhỏ nhất:
#       tường ≥ 3px, chữ ≥ 12, anchor ≥ 14px, cursor ≥ 18px)
#     - Lưới nhỏ (≤ 5×5) giữ nguyên scale = 1.0 -> không đổi gì so với trước
#     - cell.gd::set_font_size(): sửa cả LabelSettings (duplicate trước khi đổi) — trước đây
#       LabelSettings đè theme override nên cỡ chữ không đổi được
#     - Art ô + mọi trạng thái đều cùng khung 176×176 và TextureButton để stretch nên co bao nhiêu
#       vẫn khớp nhau, KHÔNG cần vẽ lại SVG theo cỡ mới
#     - Màn mẫu mới: resources/levels/level_14.tres (bàn cờ lớn 11×11) + thêm vào make_samples.py
#     - Test: scripts/test_case/test_board_fit.gd (3/5/8/11/15/20 ô: vừa khung, lưới nhỏ giữ cỡ gốc,
#       chữ/tường/anchor co theo, không ô nào tràn khung)
#     - LƯU Ý: "khung giấy" ở lần sửa trên vẫn là RECT của node Panel (1020×1020), mà art
#       game/card_board.svg có viền trong suốt + bóng đổ -> giấy thật nhỏ hơn, nên lưới vẫn
#       chạm mép giấy. Xem mục ngay dưới.
# [x] ĐÃ SỬA (2026-09): cell vẫn tràn ra mép giấy — nay đo VÙNG GIẤY THẬT của art rồi chừa margin
#     - board.gd::_read_panel_insets(): đọc texture của Panel, thu về 64×64, quét alpha > 0.05
#       để lấy bbox giấy thật, nhân ngược tỉ lệ -> Vector4(l, t, r, b) (đo được 963×968 / 1020)
#     - board.gd::panel_inner_rect(): rect giấy thật, dùng cho CẢ fit lẫn căn giữa
#     - _compute_layout(): available = inner − BOARD_PADDING(12px)×2;
#       trừ thêm overhang = max(cỡ anchor, bề rộng tường) vì tường vẽ canh TÂM mép lưới
#       -> không còn tường/anchor thò ra ngoài giấy
#     - Test bổ sung: mọi thành con (cell/tường kể cả bề rộng nét/anchor) phải nằm trong
#       panel_inner_rect() và mép chừa tối thiểu 4px; thực đo được 17–25px ở cỡ lớn
#     - Render kiểm tra mắt: 6×6 (level_12), 11×11 (level_14), 5×5 (giữ nguyên 176px)

# ----------------------------------------------------------------------------
# GOOGLE PLAY — LƯU DỮ LIỆU NGƯỜI CHƠI TRÊN ĐÁM MÂY (Play Games Saved Games)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): tầng lưu trữ đã tách backend, hiện lưu LOCAL
#     - scripts/services/save_backend.gd             (interface SaveBackend)
#     - scripts/services/local_save_backend.gd       (user:// qua DataManager)
#     - scripts/services/play_games_save_backend.gd  (STUB - chưa cài plugin)
#     - scripts/manager/SaveManager.gd               (autoload: gom blob + auto-save)
#     - scripts/utils/save.gd                        (facade tĩnh: Save.flush(), Save.reload_all())
#     - GameManager / DailyManager đã có export_progress() / import_progress() / reset_progress()
#     => Khi có plugin: CHỈ cần điền PlayGamesSaveBackend + gọi Save.use_play_games(),
#        không phải sửa manager nào khác.
#
# [ ] 1. Cài plugin Play Games Services cho Godot 4 (Android)
#        - Ví dụ: godot-play-games-services (bản Godot 4) hoặc plugin tương đương
#        - Project > Install Android Build Template
#        - Thêm plugin vào addons/, bật trong Project Settings > Plugins
#        - Kiểm tra singleton: Engine.has_singleton("GodotPlayGamesServices")
#          (nếu tên khác thì bổ sung vào PlayGamesSaveBackend.SINGLETON_CANDIDATES)
#        - File liên quan: scripts/services/play_games_save_backend.gd
#
# [ ] 2. Cấu hình Google Play Console + OAuth
#        - Tạo app, bật Play Games Services > Saved Games
#        - Tạo OAuth 2.0 client (Android) với SHA-1 của keystore release
#        - Khai báo app_id + OAuth client id vào plugin (AndroidManifest / export preset)
#        - Thêm cả SHA-1 keystore debug để test nội bộ
#
# [ ] 3. Đăng nhập + bật backend cloud
#        - Đăng nhập Play Games khi mở game (hoặc từ màn Settings)
#        - Sau khi đăng nhập: Save.use_play_games()
#        - Kiểm tra: Save.backend_id() == "play_games" và Save.is_cloud() == true
#
# [ ] 4. Hiện thực Snapshots API trong PlayGamesSaveBackend
#        - load_data(): mở snapshot SNAPSHOT_NAME -> đọc byte[] -> JSON.parse_string
#        - save_data(): JSON.stringify(blob) -> ghi byte[] -> commit snapshot
#        - clear_data(): ghi đè snapshot rỗng
#        - LƯU Ý: API bất đồng bộ -> cần signal/callback, không trả kết quả ngay
#
# [ ] 5. Xử lý xung đột local <-> cloud (QUAN TRỌNG)
#        - Blob đã có "saved_at" (unix time) + "version" do SaveManager.collect() ghi
#        - Khi 2 bên đều có dữ liệu: so sánh saved_at hoặc merge theo tiến trình cao hơn
#          (unlocked_levels / level_stars lớn hơn thì giữ)
#        - HIỆN TẠI SaveManager.set_backend() đang GHI ĐÈ bằng cloud -> phải đổi thành MERGE
#        - File liên quan: scripts/manager/SaveManager.gd (mục TODO(google-play))
#
# [ ] 6. UI trạng thái đồng bộ ở màn Settings
#        - Chip "Cloud save" đang tĩnh (scripts/scenes/settings.gd, STR_SETTINGS_CLOUD)
#        - Cần hiện: Đang kết nối / Đã đồng bộ / Lỗi + nút "Đồng bộ ngay"
#
# [ ] 7. Dọn dẹp dữ liệu local cũ sau khi cloud chạy ổn
#        - user://daily.cfg (DailyManager đọc 1 lần để DI TRÚ, sau đó xoá được)
#        - user://inkmaze_data.json giữ lại làm cache offline
#
# [ ] 8. Kiểm thử
#        - Cài lại game -> đăng nhập -> tiến trình tự về
#        - Tắt mạng -> vẫn lưu local -> có mạng -> đồng bộ
#        - 2 thiết bị cùng tài khoản: tiến trình mới nhất thắng


# ----------------------------------------------------------------------------
# KHÁC
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): Debug Console — scenes/debug.tscn + scripts/scenes/debug.gd
#     - Mở: phím F9 (input action "debug_console" do DebugManager tự đăng ký)
#       hoặc bấm 5 lần vào con dấu phiên bản ở màn Settings (bản debug)
#     - Có: xem State/Save blob, nhảy tới màn 1..9, unlock all, ghi 3 sao,
#       reset tiến trình, Daily (play/complete/reset), đổi backend lưu,
#       mở thử 5 popup, bật/tắt log theo nhóm
#     - Nội dung dựng bằng code trong debug.gd::_build() -> thêm mục mới rất nhanh
#     - Đã đưa vào test_full_flow (kiểm tra dựng được các hàng lệnh)
# [ ] Debug Console: chưa có nhóm "giả lập trong ván chơi" (thêm/xoá bước,
#     teleport, ép thắng/thua ngay trong game) — mở rộng sau nếu cần
#
# [x] ĐÃ SỬA (2026-09): Nút "Thử lại" / "Chơi lại" luôn nhảy về màn 1-1
#     - Nguyên nhân: scripts/core/controllers/game_controller.gd::_on_retry_requested()
#       gọi `start_new_run()` không tham số -> dùng mặc định start_floor = 1
#     - Sửa: thêm `GameController.current_floor()` (ưu tiên game_state.floor_number,
#       fallback GameManager.current_level khi mode không endless)
#       Nhánh fallback trong `_on_continue_requested` (không có GameManager) cũng đổi theo
#     - Test hồi quy: scripts/test_case/test_retry_floor.gd (đã verify test FAIL khi bỏ fix)
#
# [x] ĐÃ SỬA (2026-09) — PHÂN BIỆT RETRY THEO CHẾ ĐỘ (bug: Dungeon chơi lại vẫn ở tầng hiện tại):
#     - Dungeon (endless): "Chơi lại" = mở VÁN MỚI -> `start_new_run(1)` (về TẦNG 1).
#     - Play/Level + các mode khác: "Chơi lại" = chơi lại ĐÚNG màn đang chơi.
#     - Code: `GameController.retry_start_floor()` (endless -> 1 · còn lại -> current_floor()),
#       `_on_retry_requested()` dùng hàm này; áp dụng cho popup Thua · popup Thắng · nút Restart trên HUD.
#     - HỒI SINH (revive): Dungeon = +`revive_bonus_steps` bước và Ở LẠI đúng tầng;
#       Level/Play = quay lại bước trước đó (undo, không cộng bước).
#       Số bước hồi sinh nay là @export `revive_bonus_steps` (mặc định 3) chỉnh trong Inspector;
#       dòng mô tả nút HỒI SINH ở popup dùng chuỗi có tham số `STR_REVIVE_DESC_STEPS` (+{0}).
#     - Test: test_retry_floor.gd — Dungeon sau 2 tầng -> chơi lại -> tầng 1;
#       Dungeon hồi sinh -> giữ tầng + cộng đúng `revive_bonus_steps`.
#
# [x] ĐÃ LÀM (2026-09): Quy ước "số tường trong ô" — TƯỜNG VIỀN NGOÀI KHÔNG tính vào ô
#     - scripts/core/maze_data.gd::_compute_wall_counts(): bỏ cạnh thuộc viền board
#       (y==0 bỏ h[.][0] · y==height-1 bỏ h[.][height] · x==0 bỏ v[0][.] · x==width-1 bỏ v[width][.])
#     - Viền vẫn CHẶN đường đi (has_wall) và luôn hiển thị — chỉ không được ĐẾM
#     - Mọi mode hiện số (get_cell_text) tự theo; Number_Maze_Game_Design.md đã ghi rõ quy ước
#     - tools/level_designer/app/models/level.py::wall_count() đổi cùng quy ước (README + test cập nhật)
#     - Test mới: scripts/test_case/test_wall_count_rule.gd (+ tool: test_border_walls_are_not_counted)
#     - Ảnh hưởng: 66 ô trên 9 màn hiện có đổi số (ô góc có thể 2 -> 0 = không hiện số)
# [x] ĐÃ LÀM (2026-09): Countdown Cost — số trên ô CHỈ là CHI PHÍ BƯỚC (KHÔNG liên quan tường)
#     - scripts/modes/countdown_cost_game_mode.gd::_generate_costs() sinh chi phí riêng theo độ khó:
#       easy 1..2 · medium 1..3 · hard 1..4 (mọi ô >= 1, S/F không có chi phí)
#       get_cell_text() hiện đúng con số đó · get_step_cost(ô đích) = con số đó
#     - cheapest_path_cost() = Dijkstra với trọng số là chi phí ô đích;
#       _ensure_budget() đặt initial_steps = max(ngân sách tối thiểu, đường rẻ nhất + dự phòng)
#       (Easy 12/+6 · Medium 14/+4 · Hard 15/+3) => màn luôn thắng được nếu chọn đường rẻ
#     - HUD: "CHI PHÍ Ô: 1-3 BƯỚC"; Number_Maze_Game_Design.md (mục 2, bảng mục 5, mục 7) đã cập nhật
#     - Test mới: scripts/test_case/test_countdown_cost.gd (5 màn ngẫu nhiên × 3 độ khó + kiểm tra độc lập với tường)
# [ ] Màn Settings: nút "Hướng dẫn" chưa có scene (guide_requested mới chỉ push_warning)
# [ ] Popup ngôn ngữ: locale không có trong LOCALE_INFO (vd zh_TW) đang hiện mã thô
# [ ] scenes/splash.tscn và các popup cũ chưa cập nhật localization/icon SVG

# ----------------------------------------------------------------------------
# SỔ TAY THÀNH TỰU (ACHIEVEMENT / DANH HIỆU) — 2026-02
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): Màn "Sổ tay thành tựu" data-driven + phân trang
#     - Catalog = các file .tres (ArchivementData) trong resources/archivements/ (hiện 28 danh hiệu:
#       7 Màn chơi · 7 Dungeon · 7 Daily · 7 Đặc biệt, trong đó 2 danh hiệu ẨN).
#       Thêm/bớt danh hiệu = thêm/bớt file .tres, KHÔNG sửa code (ArchivementManager tự quét).
#     - scripts/manager/ArchivementManager.gd (autoload): số liệu DẪN XUẤT đọc từ GameManager/DailyManager
#       (số màn, tổng sao, số màn 3 sao, best time, số ngày/chuỗi/sao Daily) + số liệu TÍCH LUỸ do game báo về
#       qua Archivement.notify_run_result() (tầng sâu nhất, tổng tầng, điểm cao, số ván thắng, no-hint/no-undo,
#       hints/undos, hardcore, tổng thời gian chơi). Provider của SaveManager (export/import/reset_progress
#       + autosave qua signal progress_changed).
#     - scripts/utils/archivement.gd = facade tĩnh cho UI/test; GameController báo kết quả ở _complete_floor()
#       và _game_over() -> danh hiệu mở khoá ngay sau mỗi ván.
#     - scenes/archivement.tscn + scripts/scenes/archivement.gd: 5 tab lọc (TẤT CẢ · MÀN CHƠI · DUNGEON ·
#       DAILY · ĐẶC BIỆT) + 5 thẻ/trang, vuốt ngang/dots đổi trang (giống màn Chọn màn), tự nhảy tới trang
#       chứa danh hiệu đang chờ NHẬN; thẻ tổng kết (% đã mở + x/y danh hiệu + AP + dấu mộc BADGES).
#     - nodes/archivements/card.tscn: thẻ 4 trạng thái ĐÃ ĐẠT (xanh lá + dấu mộc) · NHẬN THƯỞNG (cam + nút NHẬN)
#       · ĐANG LÀM (xanh dương + chip Thưởng) · ẨN/KHOÁ (xám + ổ khoá + ??? / ???).
#     - Lối vào: Main -> nút "SỔ TAY THÀNH TỰU" (hàng Other, 4 nút 180x146) + Debug Console "Badge Book".
#     - Test mới: scripts/test_case/test_archivement.gd (54 check: catalog · tiến độ · nhận thưởng 2 lần ·
#       AP/% · export-import · 5 thẻ/trang · tabs · dots · bấm NHẬN trên thẻ) + test_main_layout.gd (nút thứ 4).
#     - Tài liệu: Number_Maze_Game_Design.md mục 8 (Sổ tay thành tựu) · archivements_list.txt (danh sách 28 danh hiệu).
# [ ] Xu chưa có chỗ tiêu (chưa có Shop/Đổi quà) — hiện chỉ tích luỹ ở ArchivementManager.coins
# [ ] Chưa có toast "Vừa mở khoá danh hiệu" khi chơi (signal achievement_unlocked đã có sẵn, chỉ cần UI)
# [ ] Hàng nút Main: Leaderboard chưa có màn hình (mới chỉ có nút) — RIÊNG XẾP HẠNG ĐÃ XONG 2026-02 (xem log cuối file)

# ----------------------------------------------------------------------------
# MAIN SCREEN: NÚT THÀNH TỰU LỚN + ĐỔI LUẬT CHƠI THÀNH CỬA HÀNG — 2026-02
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): theo yêu cầu user
#     - Bỏ nút Danh hiệu khỏi hàng Other; thêm icon huy chương LỚN 224x224 ở góc TRÊN PHẢI tờ giấy
#       (assets/images/main/btn_menu_badge_{normal,pressed,focus,disabled}.svg = sticker giấy + băng keo washi
#       + huy chương ruy băng), ô giữa huy chương hiện "x/y" danh hiệu đã mở (node Count, variation MainBadgeCount),
#       nhãn DANH HIỆU nằm dưới sticker (caption theo OtherLabel).
#     - Nút LUẬT CHƠI -> CỬA HÀNG: icon mới assets/images/icons/icon_shop.svg + chuỗi STR_BTN_SHOP (string.csv, 20 locale);
#       khung nút dùng lại btn_menu_utility_*; đã xoá btn_menu_rules.svg + archivements/icon_medal.svg (hết dùng).
#     - Hàng Other còn 3 nút, mỗi nút = ĐÚNG kích thước art 230x130 (separation 5, alignment center) — trước đây
#       để custom_minimum_size 180x146 + stretch_mode KEEP_ASPECT nên art bị co nhỏ.
#     - Test: test_main_layout.gd (btn_shop thay btn_rules; nút thành tựu >= 200px, nằm TRÊN hàng Other và bên phải;
#       hàng Other = 3 nút 230x130; texture normal/pressed/focus phải khác nhau).
# [ ] Màn CỬA HÀNG chưa có scene (nút mới chỉ chạy hiệu ứng bấm) — màn XẾP HẠNG đã xong 2026-02

# ----------------------------------------------------------------------------
# MÀN XẾP HẠNG (RANKING) — 2026-02
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): theo mockup/ranking.svg
#     - RankingManager (autoload mới): 3 bảng dungeon · play · daily; 24 đối thủ mô phỏng (RIVAL_POOL
#       {name, flag, power}) + người chơi; điểm = khoá sắp xếp duy nhất (bằng điểm thì xếp theo tên).
#       Kỷ lục người chơi lấy THẬT từ ArchivementManager.stat_value() (best_floor · best_score · levels_cleared ·
#       stars · fastest_clear · streak · daily days/stars) — chưa chơi thì hiện "CHƯA CÓ".
#     - Bảng tự "làm mới" mỗi 10 phút: seed đối thủ = hash("bảng|tên|khung-10-phút") nên ổn định giữa các phiên,
#       chỉ đổi nhẹ mỗi khung → mô phỏng bảng xếp hạng sống (refresh(force) để dựng lại ngay, last_refresh_unix()).
#     - scripts/utils/ranking.gd: facade tĩnh Ranking (test-safe) + định dạng dùng chung (record_text · points_text ·
#       thousands · display_name · rank_text) — mọi chỗ hiển thị dùng chung nên không lệch nhau.
#     - UI: scenes/ranking.tscn + scripts/scenes/ranking.gd (RankingScene) — tờ giấy 940x1570 + 3 tab + 2 đường kẻ
#       nét đứt + bục vinh quang (Gold 240x200 giữa · Silver 200x155 · Bronze 200x130, đáy thẳng hàng) + danh sách cuộn
#       (nodes/ranking/rank_row.tscn, hàng của bạn đổi art rank_row_you.svg) + thanh "hạng của bạn" dán đáy + chân trang.
#     - Asset mới: assets/images/ranking/*.svg (rank_sheet · tab_active/normal · medal_* · podium_* · rank_row(_you) ·
#       my_rank_bar · chip_scope) + theme variation Rank* trong theme_text.tres + chuỗi STR_RANK_* (string_extra.csv).
#     - Nối: SceneManager.SCENE_RANKING/goto_ranking · Nav.goto_ranking() · nút XẾP HẠNG ở Main (đã connect pressed)
#       · Debug Console NAVIGATE "Leaderboard".
#     - Test mới: scripts/test_case/test_ranking.gd (84 check) — API · sắp hạng · người chơi · ổn định · làm mới ·
#       định dạng · scene (3 tab · bục · danh sách · thanh đáy · đổi tab bằng API và bằng bấm nút · nút Back có signal).
#     - Tài liệu: Number_Maze_Game_Design.md mục 11 (Bảng xếp hạng) + cập nhật mục 4.1 và mục 7.
#     - LƯU Ý: bảng là OFFLINE/demo (chip ghi "MÁY NÀY", có dòng ghi chú "bảng ngoại tuyến") — khi nối
#       Google Play Games chỉ cần thay thân RankingManager._build_board(), UI không đổi.

# ----------------------------------------------------------------------------
# GIAO DIỆN 3 MODE MỚI (SUM PATH · COUNTDOWN COST · FADING INK) + PANEL HINT GUIDE — 2026-02
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): theo 3 mockup mới matchup_{sum_path,countdown_cost,fading_ink}.svg
#     - Khung HUD mới (2 thẻ cao 156 đặt tại y=178 trong khung Information (50,175)-(1030,424)):
#       THỜI GIAN 250x156 (card_time_tall.svg) + thẻ chế độ 715x156 tại (265,3).
#     - 3 scene HUD riêng (không còn dùng LevelHUD; KHÔNG hiện thẻ THỬ THÁCH trên HUD — thử thách/Sao
#       vẫn tính đủ và hiện ở popup kết quả):
#       * countdown_hud.tscn (CountdownHUD) — SỔ THEO DÕI NGÂN SÁCH BƯỚC CHÂN: NGÂN SÁCH CÒN (số dư/tổng) ·
#         ĐÃ TIÊU TỐN (-N bước + số ô đã đi) · GIÁ CƯỚC MỖI Ô (chip RẺ/ĐẮT theo độ khó; easy thì ẩn chip đắt ·
#         hard hiện "3-4: ĐẮT" · dự phòng +N bước) · dải phân đoạn (mỗi đoạn = 1 bước, bề rộng tự co khi > 16).
#       * fading_ink_hud.tscn (FadingInkHUD) — TRẠM ĐO ĐỘ PHAI MỰC: BƯỚC ĐÃ ĐI + (-N MỰC) · quang phổ 4 mức
#         (≥3 ĐẬM · 2 RÕ · 1 NGUY CẤP · 0 CẠN KHÓA) · cảnh báo "N Ô ĐÃ CẠN MỰC" (tự ẩn khi chưa có ô cạn).
#       * sum_path_hud.tscn (SumPathHUD) — CÂN BẰNG TỔNG ĐIỂM ĐƯỜNG ĐI: TỔNG HIỆN TẠI (+ số ô đã đi) ·
#         con dấu TOÁN TỬ (< > =) · MỤC TIÊU · chip CẦN THÊM/CÒN ĐƯỢC/ĐANG VƯỢT/ĐÃ ĐỦ · thanh tiến độ + vạch mốc.
#     - API mode thêm cho HUD: CountdownCostGameMode.cost_range()/budget_reserve() ·
#       FadingInkGameMode.count_exhausted(); UIController.update_hud(...) nhận thêm `moves`
#       (= game_state.floor_moves) để HUD hiện "đã đi N ô".
#     - PANEL HINT GUIDE: node `HintGuide` (50,1424) 980x90 trong scenes/game.tscn — nằm dưới mép giấy bàn cờ,
#       trên thanh nút, index=6 (vẫn dưới Popups); art panel_hint_guide.svg + icon_bulb.svg (KHÔNG dùng emoji);
#       nội dung theo chế độ qua khoá STR_HINT_<MODE_ID> (đủ 9 chế độ) — HintGuide.show_mode() được
#       GameScene.switch_mode() gọi; chế độ không có khoá thì fallback mode_description.
#     - Asset mới: assets/images/game/{card_time_tall · card_budget_sheet · card_ink_meter · card_sum_balance ·
#       budget_segment_on/off · chip_price_cheap/pricey · bar_sum_fill · bar_sum_ticks · bar_ink_warning ·
#       panel_hint_guide}.svg + icons/icon_bulb.svg + 32 theme variation Hud*/HintGuideText.
#     - Test mới scripts/test_case/test_hud_modes.gd (72 check): Hint Guide (9 chế độ, đo font THẬT để chắc
#       vừa 1 dòng) · Sum Path HUD (tiến độ + chip theo toán tử) · Countdown HUD (ngân sách, phân đoạn, chip giá) ·
#       Fading Ink HUD (bước/mực, cảnh báo cạn) · kích thước thẻ đúng mockup + map HUD theo mode.
#     - tools/mockup/gen_matchup.py: thêm hằng HAND_DRAWN (5 mockup vẽ tay: level · dungeon · sum_path ·
#       countdown_cost · fading_ink) → tool KHÔNG ghi đè các file này nữa.
#     - GOTCHA test: hàm có `await` bên trong là COROUTINE — gọi mà thiếu `await` thì phần SAU `await`
#       chạy lệch thời điểm (test HUD từng bắt nhầm HUD của chế độ khác).

# ----------------------------------------------------------------------------
# DEBUG CONSOLE: TEST 7 CHẾ ĐỘ SPECIAL — 2026-02
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): mục "SPECIAL MODES (TEST)" trong màn Debug (F9)
#     - GameManager: + `debug_run` (ván test) + `start_floor_override`; prepare_mode_run(mode_id,
#       difficulty, test_run, floor_override) (đặt cờ, KHÔNG đổi scene) + start_mode(...) +
#       special_mode_ids(); start_dungeon/start_level/start_daily reset 2 cờ này.
#     - game.gd::_start_floor_for() đọc `start_floor_override` -> test được ở tầng 3/5 (bàn to dần).
#     - GameController: ván test KHÔNG đánh dấu Daily (_mark_daily_completed_if_needed) và KHÔNG ghi
#       số liệu vào Sổ tay thành tựu (_report_to_archivements) — tránh phá tiến trình khi test.
#     - Debug scene: toggle Test mode · chọn độ khó (easy/medium/hard) · chọn tầng (1/2/3/5) ·
#       7 hàng vào thẳng từng chế độ (tiêu đề = Tên mode [id] · Daily ngày N, N+7…) · mô tả lấy từ
#       mode_description · "Chế độ kế tiếp" + "Chế độ ngẫu nhiên"; hàng lệnh cho mô tả xuống 2 dòng.
#     - Test mới: scripts/test_case/test_debug_modes.gd (44 check: cờ test · map id->mode · mục
#       SPECIAL MODES trong Debug scene · ván test không ghi tiến trình · ép tầng 3 -> bàn 5x5).
# [ ] Chưa có nút xem nhanh dữ liệu riêng của mode (mìn/chi phí/vùng...) trong Debug Console

# ----------------------------------------------------------------------------
# CỬA HÀNG (SHOP) — TIỆM VĂN PHÒNG PHẨM — 2026-02 (vòng 9)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): màn Cửa hàng + catalog 30 món + chuẩn bị việc apply chủ đề/ngòi bút
#     - `ShopManager` (autoload) + facade `Shop`: 4 nhóm hàng pen(10) · theme(8) · tool(6) · coin(6);
#       ví Xu Mực DÙNG CHUNG với Sổ tay thành tựu (ArchivementManager.coins) + `spend_coins()`/`notify_coins_changed()`.
#       API: items/item/has_item/price_of/category_of/is_consumable/is_equippable/can_afford/buy/use_tool/equip/
#       tool_count/purchase_coin_pack (STUB IAP)/export-import-reset_progress; signal coins_changed/item_purchased/item_equipped/tool_used.
#     - Món mặc định có sẵn: `pen_blue` (Mực Xanh Học Trò) + `theme_gride_4ly` (Vở Ô Ly 4 Ly); dụng cụ mua lại được
#       (cộng dồn lượt); gói nạp có `bonus` Xu tặng thêm; `coin_no_ads` mua 1 lần.
#     - `ThemeManager` (autoload) + facade `ThemeSkin` — CHUẨN BỊ SẴN việc apply chủ đề/ngòi bút nhưng CHƯA bật
#       (`apply_enabled = false`; `_apply_now()` là stub TODO). ĐÃ đổi tên facade từ `Skin` -> `ThemeSkin` vì
#       `Skin` TRÙNG class native của Godot (parse lỗi).
#     - Màn Cửa hàng: `scenes/shop.tscn` + `scripts/scenes/shop.gd` (ShopScene) — dựng 4 tab nhãn vở BẰNG CODE,
#       lưới 2 cột 4 thẻ/trang cho bút/giấy + danh sách thẻ dọc cho dụng cụ/nạp xu, pager, ví Xu + nút +, banner
#       tiếp sức, nút Back -> Main. 2 component `nodes/shop/item_row.tscn` (980x180) + `item_tile.tscn` (470x500).
#     - Asset mới `assets/images/shop/` (20 SVG) + 21 theme variation `Shop*` + ~90 khoá chuỗi `STR_SHOP_*`.
#     - Mockup viết lại theo MỘT bộ khung chung: `shopping_theme_page_1/2.svg` (trước đây collect từ nguồn khác,
#       tab/tiêu đề/ví khác hẳn) giờ giống `shopping_pencil/tool/coin.svg` (status bar · Back (50,85) · eyebrow +
#       CỬA HÀNG · ví 255x70 tại (775,85) · 4 tab y=185/195 + vạch đáy y=250 · banner (50,1545) · chân trang 1710).
#     - Test mới `scripts/test_case/test_shop.gd` (196 check) -> 24/24 suite PASS. Render kiểm tra 4 tab bằng
#       SubViewport 1080x1920 (phát hiện + sửa: mô tả thẻ dọc bị cắt -> cho phép 2 dòng; "Hiện có trong túi: N gói"
#       -> "Đang có: N lượt"; gói nạp không bonus bị lặp lại mô tả; tên dụng cụ dài bị ellipsis).
# [ ] Khi ghép Google Play Billing: thay thân `purchase_coin_pack()` (hiện là STUB) + nối AdsManager cho gói no_ads
# [ ] Khi muốn bật thật Theme/Pen: điền `ThemeManager._apply_now()` rồi đổi `apply_enabled = true`

# ----------------------------------------------------------------------------
# CỬA HÀNG: THẺ Ô THEO MOCKUP + TAB NẠP XU + VUỐT/CUỘN — 2026-02 (vòng 10)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-02): theo phản hồi "card item_tile khác mockup" · "nạp xu giống mockup" · "không vuốt/cuộn được"
#     - Thẻ ô bút/giấy vở dựng LẠI đúng khuôn mockup/shopping_pencil.svg: 470×500 -> **475×315**:
#       nhãn góc trên-trái (bề ngang tự co theo chữ) · VÒNG icon + icon + NÉT MỰC vẽ thử · tên 24 / mô tả /
#       dòng trạng thái canh giữa · nút **200×46** (giá Xu hổ phách + icon Xu · SỬ DỤNG tô đúng màu món hàng ·
#       ĐANG DÙNG xanh lá · món VIP nút hổ phách đặc chữ trắng).
#       Lưới 2 cột × 3 hàng = **6 ô/trang** (10 bút -> 2 trang; 8 giấy vở -> 2 trang).
#     - Tab NẠP XU làm lại theo mockup/shopping_coin.svg: gói **XOÁ QUẢNG CÁO nằm riêng TRÊN CÙNG** (`noads_row`
#       980×200, viền đỏ, nút giá 230×80, mua rồi -> "ĐÃ SỞ HỮU" + khoá) và **5 gói Xu xếp LƯỚI 2 CỘT**
#       (`coin_tile` 475×240). Mỗi gói có **ICON CẤP XU riêng** lấy từ mockup/coin_tiers.svg:
#       `icon_coin_t1..t5` = xu đơn · cọc xu · đống xu · túi tiền · rương vàng (art riêng, không modulate);
#       đã cập nhật `coin_tiers.svg` cho khớp số Xu thật (500 / 2,000 / 3,500 / 8,000 / 20,000).
#     - SỬA VUỐT/CUỘN (2 lỗi user báo): `shop.gd` tự xử lý ở `_input` vì nút trên thẻ "ăn" sự kiện kéo nên
#       ScrollContainer không tự cuộn. Vuốt NGANG -> đổi trang (>=70px, chỉ khi tab có >1 trang);
#       vuốt DỌC -> cuộn danh sách; chọn trục theo hướng di chuyển đầu tiên (ngưỡng 14px);
#       `set_input_as_handled()` khi đã nhận kéo (tránh cuộn đôi); sau khi vuốt **KHOÁ bấm nút 0.35s**
#       (`clicks_locked()`) để không mua nhầm. Content 1220 -> 1200 cao (danh sách dụng cụ dài hơn khung nhìn).
#     - Màu nhấn chủ đề đổi cho đủ tương phản trên thẻ: 4 ly #3D83AE · campus #64748B · bullet #94A3B8.
#     - Asset mới: card_coin · card_noads · icon_circle · btn_tile_{normal,done,price,price_vip} · btn_coin ·
#       btn_noads · chip_red · icon_coin_t1..t5 (đã xoá preview_frame không còn dùng).
#       Theme mới: ShopBadgeDanger · ShopBonus · ShopNoads{Title,Desc,Price}. Chuỗi mới: STR_SHOP_OWNED_BTN.
#     - Mockup viết lại đồng bộ: shopping_coin.svg (VIP + lưới 5 gói có icon cấp) ·
#       shopping_theme_page_1/2.svg (thẻ 475×315, 6 thẻ/trang) · coin_tiers.svg (số Xu khớp gói thật).
#     - Test `test_shop.gd` 196 -> **225 check** (thêm phần 7: vuốt ngang đổi trang · vuốt dọc cuộn danh sách ·
#       vuốt xong không mua nhầm · kéo ngắn không tính vuốt; section 6 kiểm thêm kích thước thẻ 475×315,
#       nút 200×46, hàng VIP 980×200 và icon cấp Xu của từng gói). Render 4 tab đối chiếu OK. 24/24 suite PASS.
# [ ] Bàn nháp "thử bút" (980×215 trong shopping_pencil.svg) chưa làm — chưa được yêu cầu
# [ ] Tab DỤNG CỤ vẫn là thẻ dọc 980×180; muốn đồng bộ thẻ ô 475×315 thì báo thêm

# ----------------------------------------------------------------------------
# SỬA ASSET/POPUP BỊ LỖI — 2026-09 (vòng 11)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): theo 3 ảnh user gửi (nút Restart · popup thắng màn · popup ngôn ngữ)
#     - ICON NÚT RESTART (HUD) bị lỗi: mũi tên cũ là 1 đường gãy `M 18.5 22 L 27.5 22 L 23 29.8`
#       nằm LỆCH ra ngoài cung tròn -> trông như hình hỏng. Đã thay cả 4 trạng thái
#       (btn_restart_{normal,pressed,focus,disabled}.svg) bằng đúng hình mockup/matchup_level.svg:
#       cung `M 50 25 A 15 15 0 1 0 52 42` + mũi tên `42,20 52,24 50,34`.
#     - NÚT CHƠI LẠI (popup thắng màn) KHÔNG có icon: node TextureRect sẵn có bị để trống.
#       Thêm asset `assets/images/popups/icon_replay.svg` (cùng hình mũi tên vòng) + gắn vào nút,
#       canh giữa theo bề ngang nút (76,14)-(116,54) trên nút 192×108.
#     - CON DẤU (stamp) thiếu chữ: art `stamp_excellent.svg` / `stamp_passed.svg` có `<text>` đã bị
#       COMMENT (và ThorVG cũng KHÔNG render `<text>`), nên trước đây chỉ thấy vòng nét đứt.
#       Nay thêm 2 Label con cho node `Stamp`:
#         · winning: StampTitle = STR_WIN_STAMP_TITLE "{0} / {1} THỬ THÁCH" (màu đỏ, 20px),
#           StampSub = STR_WIN_STAMP_SUB "★ ĐẠT {0} SAO ★" (14px) — điền từ data `challenges` + `stars`.
#         · next_floor: StampTitle = STR_RESULT_STAMP_PASSED "ĐÃ QUA", StampSub = STR_RESULT_STAMP_FLOOR
#           "TẦNG {0} ✔" (%02d, màu xanh lục). ĐỒNG THỜI sửa khung con dấu bị méo: node `Stamp` cũ dùng
#           anchor + offset rác (offset_right = 0.049) -> cao 0.1px; nay là (592,47) 116×116.
#     - POPUP NGÔN NGỮ hiển thị lỗi + không vuốt cuộn được:
#         · Hàng nút tràn ra ngoài phiếu: nút HỦY BỎ đang 300×88 trong khi art là 180×96 và khung nội
#           dung chỉ 584px (180+30+390 = 600) -> nay HỦY BỎ = 180×96 + `Content` rộng 600 (offsets 110/-70).
#         · Không vuốt cuộn: hàng ngôn ngữ là TextureButton nên "ăn" sự kiện kéo, ScrollContainer không
#           nhận được. `language_popup.gd` nay tự xử lý kéo ở `_input` (ngưỡng 14px, `set_input_as_handled()`,
#           khoá bấm hàng 0.35s sau khi vuốt để không chọn nhầm ngôn ngữ) — giống cách màn Cửa hàng làm.
#     - SỬA THÊM (cùng loại lỗi `<text>`): logo `assets/images/main/logo_doodle_maze.svg` có chữ S/F
#       vẽ bằng `<text>` -> mất chữ trong game; nay vẽ bằng path (S xanh ở góc trên-trái, F hổ phách góc trên-phải).
#     - 2 LỖI PHÁT HIỆN KHI CHẠY SUITE (do bản chỉnh trước đó):
#         · `string_extra.csv` thiếu `STR_SHOP_COIN_NOADS_DESC` (catalog còn trỏ tới) -> đã thêm lại.
#         · `chapter_card.tscn`: doodle dời sang x=51 nhưng ổ khoá vẫn ở 122 -> lệch 17px; đã dời ổ khoá
#           về x=105 (đúng tâm doodle) cho khớp luật "thân khoá trùng tâm doodle".
#         · Thẻ gói nạp: mô tả mới của user đã ghi "Tặng thêm 200 Xu" nên dòng ưu đãi riêng bị LẶP;
#           `coin_tile.gd` nay chỉ hiện dòng ưu đãi khi mô tả CHƯA nhắc số Xu thưởng.
#     - Test mới `scripts/test_case/test_popup_ui.gd` (45 check) + chạy lại toàn bộ: 26/26 SUITE PASS.

# ----------------------------------------------------------------------------
# POPUP NGÔN NGỮ: ĐỦ 19 NGÔN NGỮ + CỜ RIÊNG — 2026-09 (vòng 12)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): user báo "popup ngôn ngữ vẫn sai: nút ÁP DỤNG tràn, nhiều ngôn ngữ không có
#     dịch thuật + không có cờ".
#     - NGUYÊN NHÂN: `LocalizationManager.LOCALE_INFO` chỉ khai 6 ngôn ngữ (vi · en · ja · ko · zh_CN · fr)
#       trong khi `string.csv` có **19 locale** -> 13 ngôn ngữ còn lại rơi vào fallback: hiện MÃ THÔ
#       ("MS", "PT_BR", "RU", ...) + cờ dự phòng `flag_generic.svg`. Thư mục cờ chỉ có 7 file.
#     - Đã khai đủ **19 ngôn ngữ** trong LOCALE_INFO: en · vi · zh_TW · zh_CN · es · ar · de · fr · hi · id
#       · it · ja · ko · ms · pt · pt_BR · ru · th · tr (tên bản địa + phụ đề tiếng Anh + mã quốc gia).
#       Thêm `LOCALE_ALIASES` (zh_cn/zh_tw/pt_br) cho an toàn nếu mã viết khác hoa-thường.
#     - Vẽ thêm **13 lá cờ** (assets/images/icons/flags/): flag_zh_tw · flag_es · flag_ar · flag_de · flag_hi ·
#       flag_id · flag_it · flag_ms · flag_pt · flag_pt_br · flag_ru · flag_th · flag_tr — cùng khuôn 38×28,
#       nền bo góc + viền #224C6D, dải màu vẽ bằng path bo góc (không dùng clipPath).
#     - NÚT ÁP DỤNG "tràn": đo lại đúng luồng (mở popup qua PopupManager trong màn Settings) —
#       `apply` = (470,1321) 390×96 -> mép phải 860 < mép phiếu 938 => **KHÔNG tràn** (đã sửa từ vòng 11:
#       HỦY BỎ 180×96 + `Content` rộng 600). Ảnh user gửi là bản TRƯỚC khi sửa; nếu còn thấy lệch thì do
#       cửa sổ game chưa nạp lại (project dùng stretch `canvas_items` + aspect `expand` nên không bị cắt).
#     - Test `test_popup_ui.gd`: 45 -> **52 check**, thêm mục 6 — đủ 19 mã ngôn ngữ · mỗi ngôn ngữ có TÊN riêng
#       (không hiện mã thô) · FILE cờ tồn tại · không ngôn ngữ nào dùng cờ 'generic' · mỗi HÀNG trong popup
#       có tên + cờ riêng. Toàn bộ **26/26 suite PASS**.
#     - Render đối chiếu: bảng 19 cờ + tên bản địa OK; popup trong màn Settings: 6 hàng đầu hiện
#       English · Tiếng Việt · 繁體中文 · 简体中文 · Español · العربية kèm cờ đúng.
# [ ] Cờ 'ar' hiện là cờ giản lược (nền lục + trăng sao) — muốn dùng cờ 1 nước cụ thể thì báo

# ----------------------------------------------------------------------------
# NÚT POPUP NGÔN NGỮ DÙNG SAI RESOURCE (TRÀN) — 2026-09 (vòng 13)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): user nhấn mạnh "button overflow do dùng SAI RESOURCE".
#     - TÌM RA THỦ PHẠM: `TextureButton` mặc định **stretch_mode = STRETCH_KEEP (2)** — art được vẽ
#       ĐÚNG KÍCH THƯỚC GỐC, KHÔNG co theo node. Popup ngôn ngữ đang gán `btn_popup_wide_*` (art **630×96**,
#       vốn là nút CTA rộng của popup khác) cho nút rộng 390 → art thò ra 2 bên, kéo thành vệt xanh tới mép màn.
#       Nút HỦY BỎ cũng sai: dùng `btn_popup_muted_*` (300×88) cho nút 180×96.
#     - SỬA: tạo BỘ ART RIÊNG đúng cỡ theo mockup `popup_language.svg`:
#         · `popups/btn_lang_cancel_{normal,pressed,focus,disabled}.svg` — **180×96**, giấy #FEFDFA + viền #BAC7CF 2.5, rx 16.
#         · `popups/btn_lang_apply_{normal,pressed,focus,disabled}.svg` — **390×96**, mực #3D83AE + viền #256286 3.5, rx 18.
#       Gắn vào `language.tscn` (giữ `ignore_texture_size = true` + custom_minimum_size 180×96 / 390×96).
#     - Đo lại sau khi sửa: nút Cancel 180×96 (art 180×96) · Apply 390×96 (art 390×96) — khớp 1:1;
#       quét pixel hàng nút: vệt xanh xa nhất **x=856 < 860** (mép nút) => HẾT TRÀN.
#     - QUÉT TOÀN BỘ POPUP (script tạm) tìm nút lệch art: chỉ còn `next_floor/GoBtn` (art 425×96 trong node
#       437×108 — art NHỎ hơn nên canh giữa, không tràn) và 2 checkbox ở `pause` (art 50×50 trong node 44×44,
#       `stretch_mode = 0` nên tự co — an toàn).
#     - Test `test_popup_ui.gd` 52 -> **54 check**: thêm kiểm tra ART của HỦY BỎ / ÁP DỤNG phải khớp 1:1 với nút
#       (chống tái phát đúng lỗi này). Toàn bộ **26/26 suite PASS**.
#     - Ghi vào tài liệu §5.11: quy ước "art nút phải khớp 1:1 với node" + bảng art nút popup ngôn ngữ.

# ----------------------------------------------------------------------------
# HOTFIX 7 LỖI LAYOUT TÌM THẤY KHI TEST TRÊN EMULATOR ĐIỆN THOẠI (Pixel_9_API_35) — 2026-09 (vòng 14)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09): sửa theo `hotfix/need_to_fix.txt` + 5 ảnh user gửi.
#   1. MÀN GAME — popup "Game Over" nháy hiện rồi BIẾN MẤT (Ảnh 1):
#      · Thủ phạm: `ui_controller.show_game_over()` gọi `Popups.close_all()` rồi MỞ LẠI CÙNG ID khi tween đóng
#        của popup cũ vẫn đang chạy → callback `_finish_close()` xoá luôn popup vừa mở. Ngoài ra `_game_over()`
#        bị gọi 2 lần (đâm tường + hết giờ trong cùng khung hình) nên mở 2 popup trùng id.
#      · Sửa `scripts/nodes/popups/base.gd`: giữ `_tween` + `_kill_tween()` khi bắt đầu hiệu ứng mới;
#        `close()` chặn đóng trùng (`if _closing: return`); `_finish_close()` chỉ xoá khi VẪN đang đóng
#        (`if not _closing: return`) → mở lại giữa lúc đang đóng thì popup vẫn sống, không bị "nháy".
#      · Sửa `scripts/core/controllers/game_controller.gd::_game_over()`: thêm `if not _run_active: return`.
#      · Kiểm chứng: `dev_popup_repro.gd` thêm 2 ca ("gọi 2 lần liên tiếp" + "mở lại giữa lúc đang đóng")
#        → 4 PASS · 0 FAIL; trên emulator: đâm tường vô hình → popup "ĐÂM VÀO TƯỜNG VÔ HÌNH!" vẫn hiện lâu dài.
#   2. MÀN GAME — lưới ô KHÔNG nằm giữa thẻ bàn (Ảnh 2): `BoardView` lưu insets của `card_board.svg` theo PIXEL
#      nhưng dùng như đơn vị panel-local → panel cao 1288 (màn 1080×2424) thì vùng lưới tính thiếu, lưới đẩy LÊN.
#      Sửa `scripts/nodes/game/board.gd`: insets đổi sang TỈ LỆ 0..1, `panel_inner_rect()` nhân với `panel.size`.
#   3. MÀN GAME — HUD lệch lên trên: gốc `Hud` neo offset thiết kế (y=175) nên dính sát status bar trên màn cao.
#      Sửa `nodes/hud/base.tscn`: `anchor_top/bottom = 0.072` + offset giữ khoảng cách 37px dưới status bar.
#   4. MÀN GAME — popup không ở giữa: `BasePopup._apply_canvas_layout()` chỉ canh giữa NGANG. Sửa: canh giữa
#      cả 2 chiều theo canvas (màn 2424 thì thẻ nằm giữa màn hình, không dính lên trên).
#   5. MÀN GAME — text nút "Vẽ đường"/"Ghi nhớ" dùng STR_ID + thêm dòng phụ: `scenes/game.tscn` chuyển sang
#      `STR_TOOL_DRAW_PATH` / `STR_TOOL_MARK_WALL`, thêm node `Sub` (`STR_TOOL_DRAW_PATH_DESC` /
#      `STR_TOOL_MARK_WALL_DESC`) + LabelSettings mới `resources/settings/text/text_game_button_sub.tres`.
#   6. MÀN DAILY — Mission lệch & đè nhau (Ảnh 4): Calendar bị anchor giãn thành 1275 (đè 350px lên Missions)
#      và còn 692px trống dưới. Sửa `scenes/daily.tscn` (Calendar cao cố định 1010; Missions `expand_mode = 1`
#      để không bị ảnh 592px chặn co) + `scripts/scenes/daily.gd::_layout_responsive()`: Missions nằm giữa
#      Calendar và nút CHƠI neo đáy; co hàng khi màn thấp, giãn nhẹ (≤1.35×) khi màn cao; dời hàng tiến độ theo.
#   7. MÀN SHOP — khoảng trống dưới (Ảnh 5): neo `Content`/`Pager`/`GiftBanner`/`Footer` vào ĐÁY canvas
#      (`scenes/shop.tscn`) + `scripts/scenes/shop.gd`: số ô mỗi trang tính từ chiều cao THẬT của khung cuộn
#      (trừ chiều cao `doodle_pad` khi ở tab BÚT & MỰC) → màn điện thoại 8 ô/trang (2 trang cho 10 bút),
#      màn ngang 6 ô/trang; đổi hướng màn hình thì phân trang lại và GIỮ trang theo item đang xem
#      (`_page_first_id`) — nội dung đủ chỗ thì hiển thị luôn trên trang, không sinh trang mới.
#   8. Test/công cụ: thêm `scripts/test_case/dev_back_probe.gd`; `dev_aspects.gd` nay set `current_scene` khi
#      thêm scene vào cây (vì `PopupManager.get_host()` tìm host theo `current_scene` → trước đây 10 check
#      "bấm pause không mở được popup" FAIL oan trong harness). Kết quả: 29/29 suite PASS · tỉ lệ 220/220 PASS.
#   9. GHI NHẬN CÒN LẠI (chưa sửa, chờ user quyết):
#      · Nút Back trên Android đang THOÁT APP khi màn chơi đang mở popup (project để mặc định
#        `quit_on_go_back = true`; kể cả Windows, Esc lần 1 bị nuốt, Esc lần 2 mới đóng popup).
#      · Màn chính còn khoảng trống dưới thanh điều hướng trên màn dọc cao (nav ở y≈1800 / canvas 2424).
#      · Còn các `print()` debug của user: `Close Popup …`, `Show Game Over because …`, `GAME_OVER because …`.

# ----------------------------------------------------------------------------
# SHOP: TAB & THẺ Ô CAO ×1,5 + TỰ CO GIÃN THEO MÀN HÌNH · GAME: TRÍCH KHỐI HUD SHEET RA TEXTURE NODE — 2026-09-17 (vòng 15)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09-17): theo yêu cầu user (2 màn: Shop + Game).
#   1. SHOP — TAB cao ×1,5 + tự resize theo màn hình:
#      · Art viết lại cao gấp 1,5: `assets/images/shop/tab_active.svg` 240×**98** (65×1,5),
#        `tab_inactive.svg` 240×**83** (55×1,5) — giữ nguyên màu/bo góc/lề.
#      · `scripts/scenes/shop.gd`: `screen_scale()` = clamp(cao_canvas/1920, 0,85…1,5) ·
#        `tab_height()` = 98 × hệ số · `_apply_tab_metrics()` dàn hàng tab: tab ĐANG CHỌN cao hết hàng
#        (nhô lên), tab chưa chọn thấp hơn + canh ĐÁY hàng (`SIZE_SHRINK_END`), chữ của MỌI tab cùng một
#        đường ngang (theo tâm tab đang chọn), `TabLine` chạy theo đáy hàng, `Content.offset_top` đi theo.
#      · Đỉnh hàng tab 185 → **172** và khe Content 17 → **8** để hàng tab cao thêm 30px mà KHÔNG lấn
#        vùng danh sách (tab DỤNG CỤ 6 hàng = 1180 vẫn vừa khung 1192 như thiết kế 1920).
#      · `stretch_mode = STRETCH_SCALE` (art lấp đầy node) — vì art không cùng tỉ lệ với node nữa.
#   2. SHOP — THẺ Ô (Bút & Mực · Giấy vở) cao ×1,5 + tự resize theo màn hình:
#      · `tile_height()` = 294 × 1,5 × hệ số màn hình (điện thoại 1080×2424 → **556**; canvas 1920 → 441).
#      · `shop.gd` đặt `custom_minimum_size` cho từng thẻ khi dựng lưới + `_apply_card_metrics()` khi xoay màn hình;
#        số món/trang vẫn tính từ chiều cao THẬT của khung cuộn (điện thoại: 4 thẻ/trang – 10 bút = 3 trang).
#      · `scripts/nodes/shop/item_tile.gd`: `_apply_layout()` (chạy khi RESIZED/READY) dàn lại khối bên trong
#        theo chiều cao THẬT: nút hành động neo ĐÁY (cách 16), khối chữ (Name/Desc/Note) nằm ngay trên nút,
#        cụm icon (vòng tròn 96×88 + nét mực 200×44) canh giữa khoảng trống còn lại, lề trái giãn hết thẻ.
#        Ở cỡ thiết kế 294 các công thức cho ra ĐÚNG toạ độ cũ (đã đối chiếu từng số trong scene).
#   3. GAME — TRÍCH KHỐI TRONG 3 SHEET HUD RA TEXTURE NODE:
#      · `card_budget_sheet.svg` (Countdown Cost) → 3 art mới: `card_budget_block_budget.svg` (217×84),
#        `card_budget_block_spent.svg` (202×84), `card_budget_block_price.svg` (197×84);
#        sheet giữ khung + lề đỏ + dòng kẻ + vạch ngăn cách.
#      · `card_ink_meter.svg` (Fading Ink) → `card_ink_block_bottle.svg` (50×68) + `card_ink_swatch_1..4.svg`
#        (90/90/90/100×28); sheet giữ khung + lề mực + dòng kẻ + vạch chia dọc.
#      · `card_sum_balance.svg` (Sum Path) → `card_sum_block_current.svg` (252×76) · `card_sum_emblem.svg` (60×60)
#        · `card_sum_block_target.svg` (307×76) · `card_sum_chip_need.svg` (155×26) · `card_sum_bar_track.svg` (654×18);
#        sheet chỉ còn khung + lề đỏ + dòng kẻ.
#      · MẸO ĐỊNH VỊ: mỗi art viết với `viewBox` = VÙNG đã cắt (giữ NGUYÊN toạ độ gốc của sheet) nên chỉ cần
#        đặt node tại "sheet-local + (265, 3)" là khớp 1:1 — không phải tính lại toạ độ từng hình.
#        (Lề 1px quanh hình để nửa ngoài của `stroke` không bị viewBox cắt.)
#      · Node mới trong `nodes/hud/countdown_hud.tscn` (BlockBudget/BlockSpent/BlockPrice),
#        `fading_ink_hud.tscn` (BlockBottle/Swatch1..4), `sum_path_hud.tscn` (BlockCurrent/Emblem/BlockTarget/
#        ChipNeed/BarTrack) — chèn NGAY SAU `Sheet` để nằm trên sheet và DƯỚI các Label/nút.
#   4. Kiểm chứng: `test_shop.gd` cập nhật theo cỡ động (249/249 check) · `test_hud_modes.gd` 101/101 ·
#      **29/29 suite PASS** · harness tỉ lệ **220/220 PASS** · render lại shop (1080×2400 · 1080×1920 · 2400×1080)
#      + cắt 1:1 khung HUD 3 chế độ để soi pixel · APK build lại và mở trên máy ảo Pixel_9_API_35
#      (tab cao hơn, thẻ cao hơn, TRANG 1/3 — 4 thẻ/trang, banner cuối trang vẫn đúng).
#   5. Công cụ: `dev_fix_shots.gd` thêm `_game_mode()` + `_hud_shot()` (chụp riêng khung HUD 980×249 ở 1:1).

# ----------------------------------------------------------------------------
# SHOP: THẺ Ô VỀ ×1,25 · MỌI CỠ LẤY TỪ LAYOUT/ART (hết hard-code size trong script) — 2026-09-17 (vòng 16)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM (2026-09-17): theo yêu cầu user "chỉnh lại Card ... thành 1.25" + "lấy từ Layout của card item
#     để tôi có thể dễ dàng edit/chỉnh lại size" + "làm như thế với các node đang dùng code cứng làm size
#     mà không dùng transform".
#   1. THẺ Ô = LAYOUT CỦA SCENE:
#      · `nodes/shop/item_tile.tscn`: MỌI khối con chuyển sang **ANCHORS** (không còn dàn bằng code):
#        Bg full-rect · Bar neo 2 đầu (18px trên/dưới) · IconCircle/Icon/Stroke neo theo TỈ LỆ chiều cao
#        (29,93197% / 29,2517% / 46,93877%) · Badge neo trên · Name/Desc/Note/Action/ActionLabel/CoinIcon neo ĐÁY
#        (cách đáy 106/84/64 và 62/16 — đúng bằng toạ độ thiết kế cũ ở H=294).
#      · `scripts/nodes/shop/item_tile.gd`: XOÁ `_apply_layout()/_set_rect()/_notification()` + mọi hằng số
#        kích cỡ; script chỉ còn nội dung/nút (không đụng tới size).
#      · `shop.gd`: `TILE_SCALE` 1,5 → **1,25**; thêm `tile_design_size()` (đọc cỡ thiết kế từ scene 1 lần),
#        `tile_size()` = (rộng thiết kế, cao thiết kế × 1,25 × hệ số màn hình), `tile_height()`.
#        → canvas 1920: 475×368 · điện thoại 1080×2424: 475×464 (trước là 557).
#   2. TAB = ART LÀ NGUỒN SỐ THẬT: `tab_height()` đọc `tab_active.svg` (98) × hệ số màn hình;
#      tỉ lệ tab chưa chọn = `tab_inactive.svg` (83) / `tab_active.svg`; số đo hàng tab (đỉnh · bề rộng · khe ·
#      chiều cao vạch kẻ · khe tới danh sách) đọc từ `shop.tscn` bằng `_capture_tab_layout()` — hết hằng số trong script.
#   3. DAILY = SCENE LÀ NGUỒN SỐ THẬT: `daily.gd` thêm `_capture_design()` (đọc 1 lần): chiều cao bảng (Missions),
#      đầu hàng (Rows), đỉnh hàng tiến độ (ProgressLabel), khe lịch→bảng, lề dưới nút CHƠI (theo chiều cao canvas
#      thiết kế trong project.godot) và chiều cao hàng (`mission_row.tscn`); bảng tối thiểu suy từ các số đó.
#      CHÍNH SÁCH còn lại trong script: `MIN_ROW_FACTOR` 0,7 · `MAX_ROW_FACTOR` 1,35 (co/giãn hàng).
#      `scenes/daily.tscn` sửa lại cho KHỚP thiết kế runtime: Missions (30,1206)-(1050,1798) và Rows bắt đầu 112.
#   4. Kiểm chứng: `test_shop.gd` **252/252** (thêm check "cỡ thẻ lấy từ item_tile.tscn", "chiều cao tab lấy từ
#      art", "tab đang chọn cao hơn tab chưa chọn", "nút neo đáy thẻ") · `test_daily.gd` 87/87 ·
#      **29/29 suite PASS** · harness tỉ lệ **220/220 PASS** · render lại shop/daily/HUD · APK build lại và
#      xác nhận trên máy ảo Pixel_9_API_35 (tab cao hơn, thẻ 1,25×, TRANG 1/3, banner cuối trang).
#   5. LƯU Ý: harness `dev_aspects.gd` mục "game/popup" có lần flake 8/220 ("thẻ popup lệch tâm 537 vs 540")
#      rồi chạy lại PASS 220/220 — nghi do đo giữa lúc hiệu ứng/tải máy, không phải lỗi layout.
#
# ============================================================================
# QUY ƯỚC: MỌI NODE UI PHẢI CÓ SCENE (.tscn) + SCRIPT RIÊNG — KHÔNG `.new()` TRONG CODE (vòng 17)
# ============================================================================
# Yêu cầu user: "tránh trường hợp tạo ra scene trong quá trình khởi động mà không có tscn/script tương ứng (VD: tạo
# cell trong khởi động là được, nhưng tạo tab mà không có scene, chỉ có đoạn script added node vào bên trong control
# thì không được). Đối với trường hợp đó, bạn hãy tạo ra một scene và script của nó và rồi trong script mới khai
# khởi tạo nó."
#
# 1. SHOP — 3 scene con mới (`nodes/shop/`): `tab_button.tscn` (+ `scripts/nodes/shop/tab_button.gd`,
#    `class_name ShopTabButton`: art + cỡ 240×98 nằm trong scene · `art_height()`/`inactive_ratio()` đọc từ art ·
#    `set_active()` · `apply_row_layout()` canh đáy hàng), `item_grid.tscn` (`ShopItemGrid` — 2 cột, khe 30/24 trong
#    scene), `page_dot.tscn` (`ShopPageDot.set_current()` — 34×24 / 12×24).
# 2. SỔ TAY — 3 scene: `nodes/archivements/tab_button.tscn` (`AchTabButton`: 134×46 + nhãn · `setup()` ·
#    `set_active()` đổi art + modulate nhãn), `page.tscn` (`AchPage`: con `Column` VBox — khe 20 nằm trong SCENE,
#    hết `CARD_PITCH` trong script), `page_dot.tscn` (`AchPageDot`).
# 3. CHỌN MÀN — 2 scene: `nodes/level_selection/page.tscn` (`LevelsPage`: con `Grid` 3 cột · khe 46/24 · gốc lưới
#    10.08/11.04 — hết hằng GRID_* trong script) + `page_dot.tscn` (`LevelsPageDot` + `is_current()`).
# 4. XẾP HẠNG — `nodes/ranking/tab_button.tscn` (`RankTabButton`: cao 52 theo scene, bề rộng riêng từng bảng ·
#    `setup()` · `set_active()` đổi art + màu nhãn).
# 5. POPUP — `nodes/popups/host.tscn` (`PopupHost`: lớp phủ khi scene không có node "Popups", thay `Control.new()`).
# 6. CÁC CHỖ CÒN LẠI đã chuyển sang scene: `nodes/game/board_layers.tscn` (`BoardLayers`: 5 lớp vẽ Cells/Lines/
#    Walls/Anchors/Markers thay 5 `Control.new()`), `nodes/game/ink_footstep.tscn` (`InkFootstep.setup()` — vệt mực
#    bước chân), `nodes/hud/countdown_segment.tscn` (`CountdownSegment.set_width()/set_used()` — vạch dải ngân sách),
#    `nodes/popups/language_row.tscn` (`LanguageRow.setup()/set_selected()` — hàng ngôn ngữ).
# 7. BÀI HỌC (quan trọng): trong `.tscn`, node CON của root PHẢI ghi `parent="."` — thiếu dòng này scene vẫn load
#    nhưng con KHÔNG được gắn → `$Column`/`$Grid`/`$Label` trả về null (test Sổ tay + Chọn màn FAIL oan).
#    Ngoài ra `@onready` chỉ có giá trị SAU khi node vào cây → phải `add_child()` TRƯỚC rồi mới `setup()`
#    (đã dính ở `InkFootstep`: "Invalid assignment ... 'texture' ... on a base object of type 'Nil'").
# 8. KIỂM CHỨNG: `scripts/test_case/test_ui_scenes.gd` (MỚI, **50/50**): dựng từng scene con + kiểm cấu trúc con,
#    API setup/set_active/set_current, và QUÉT MÃ NGUỒN (`scripts/scenes` + `scripts/nodes`) — không script UI nào
#    còn `.new()` cho lớp UI (ngoại lệ: `debug.gd`). Toàn bộ suite PASS · harness tỉ lệ **220/220 PASS** ·
#    `test_shop.gd` 252/252 · `test_archivement.gd` · `test_levels_paging.gd` · `test_ranking.gd` đều PASS.
# 9. SỬA KÈM: `nodes/shop/item_tile.tscn` root `offset_bottom` 364 → **294** (khớp `custom_minimum_size`; trước đó
#    `tile_design_size()` đọc ra 475×364 làm `test_shop` FAIL 2 check).
#
# CÒN LẠI (chưa làm): `scripts/scenes/debug.gd` (màn DEBUG dev-only) vẫn tạo ~20 node bằng `.new()`
# (row · chip · label · button · grid…) — chưa chuyển thành scene vì đây là công cụ dev, không phải UI người chơi.
#
# ============================================================================
# FOG OF WAR: 3 LƯỢT THỬ LẠI + HUD "BẢNG SƯƠNG MÙ" THEO MOCKUP MỚI (vòng 18)
# ============================================================================
# Yêu cầu user: "update giao diện của Chế độ Fog Of War, và cả thay đổi thêm là được 3 lượt thử, sau khi đâm vào
# tường 3 lần thì trò chơi kết thúc, khi sử dụng chức năng hồi sinh thì sẽ được thêm 1 lượng thử nữa".
#
# 1. LUẬT MỚI — 3 LƯỢT THỬ LẠI:
#    · `BaseGameMode`: thêm `max_retries`/`retries_left` + `uses_retries()` · `reset_retries()` ·
#      `register_hazard()` (trả về true nếu HẾT LƯỢT -> thua) · `on_revive()` (hồi sinh +1 lượt) ·
#      hook `on_respawned()`.
#    · `FogOfWarGameMode`: `RETRY_LIVES = 3`; `instant_game_over_on_hazard = false` (đâm tường KHÔNG thua ngay);
#      `setup_floor()` nạp lại 3 lượt; đâm tường -> về ô S + trừ 1 lượt; hết lượt -> GameController mở popup thua.
#    · `GridController`: sau khi đưa nhân vật về S phải gọi `mode.on_respawned()` (lỗi cũ: sương mù vẫn tính theo
#      vị trí CŨ sau khi đâm tường/hồi sinh).
#    · `GameController`: `_on_step_consumed` dùng `register_hazard()`; `revive_run()` gọi `on_revive()` + đồng bộ
#      hiển thị qua `on_respawned()`; dữ liệu popup thua thêm `max_retries`/`retries_left`.
#    · `GameOverLevelPopup`: khi `max_retries > 0` -> dòng dưới nút HỒI SINH đổi sang `STR_REVIVE_DESC_RETRY`
#      ("Nhận thêm +1 LƯỢT THỬ để dò tiếp").
# 2. HUD MỚI `nodes/hud/fog_of_war_hud.tscn` (+ `FogOfWarHUD`) theo `mockup/matchup_fog_of_war.svg`:
#    THỜI GIAN 250×156 (chip "ĐANG DÒ ĐƯỜNG") + BẢNG SƯƠNG MÙ 690×156 (❤️ LƯỢT THỬ LẠI "n/3" + "CÒN n LẦN VỀ S" ·
#    hàng 👁️ TẦM NHÌN + chip XUNG QUANH · 2 dòng cảnh báo luật). KHÔNG có thẻ THỬ THÁCH (`challenge_card() = null`).
#    Art mới: `card_fog_sheet.svg` 690×156 · `card_fog_row_vision.svg` 425×42 · `card_fog_row_warn.svg` 425×64 ·
#    `card_fog_chip_blue.svg` 105×26 · `card_fog_chip_time.svg` 135×24. Theme thêm 7 variation `HudFog*`.
#    `GameScene`: `HUD_FOG_OF_WAR` + map `fog_of_war` -> `FogOfWarHUD`; nhãn PHỤ 2 nút công cụ đổi theo chế độ
#    (`TOOL_SUB_KEYS` — Fog: "Dò trong sương" / "Cắm cờ mép ô").
# 3. Chuỗi dịch mới (`string_extra.csv`): STR_HUD_FOG_* · STR_REVIVE_DESC_RETRY · STR_TOOL_*_FOG; cập nhật
#    STR_HINT_FOG_OF_WAR cho luật mới.
# 4. Kiểm chứng: `test_fog_of_war.gd` (MỚI, **28/28**: 3 lượt · về S · hết lượt -> popup thua · hồi sinh +1 ·
#    đâm tiếp lại thua · HUD + nhãn nút) · `test_hud_modes` thêm mục 4c (HUD fog) · harness tỉ lệ OK ở cả 10 cỡ
#    (ảnh `tmp_aspect/<size>/game_fog_of_war.png`).
# 5. LƯU Ý: `test_hud_modes` (7 check) + `test_challenge_controller` (3 check) ĐANG FAIL ở HUD **Sum Path** và
#    **Fading Ink** — do `nodes/hud/sum_path_hud.tscn` + `fading_ink_hud.tscn` đã bị đưa về BẢN CŨ (mất các khối
#    tách riêng + anchors khác) trong khi test viết theo bản tách khối. CHƯA sửa — chờ quyết định của user.

# ----------------------------------------------------------------------------
# ONE STROKE: MODE + HUD "BẢNG TIẾN ĐỘ PHỦ KÍN" + POPUP HƯỚNG DẪN 3 TRANG (vòng 19 — 2026-09-19)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("apply One Stroke và Instruction của nó" — 7 SVG user gửi):
# 1. LUẬT CHỐT THEO MOCKUP (2 điểm KHÁC §5.12 cũ — đã sửa lại trong số tay §13.18):
#    · Ô đã đi qua bị khoá VĨNH VIỄN: kéo nét đè lên = **THUA NGAY** (hazard `revisit`) — không phải "bị chặn".
#    · Bàn chỉ dùng cỡ **LẺ**: easy 3×3 · medium 5×5 · hard 7×7 (S=(0,0) → F=(n-1,n-1) cùng màu đa số ⇒ có đường Hamilton).
#    · Chạm F khi còn ô khác chưa đi ⇒ **BỊ CHẶN** (không thua) + chữ nổi "CÒN n Ô CHƯA ĐI!". F là ô cuối cùng.
#    · Ô trống còn lại bị cắt rời ⇒ thua "HẾT ĐƯỜNG ĐI!" (`STR_GAME_OVER_NO_PATH`). Đi lại ô cũ ⇒ thua "ĐI LẠI Ô CŨ!"
#      (`STR_GAME_OVER_REVISIT`). Undo mở lại ô vừa rời. Hint soi ô đúng kế tiếp.
# 2. `scripts/modes/one_stroke_game_mode.gd` (`OneStrokeGameMode`):
#    · Sinh bàn: dựng **đường rắn phủ kín** từ S tới F TRƯỚC, rồi rải tường ngẫu nhiên (18/34/50%) **chỉ trên các cạnh
#      KHÔNG thuộc đường mẫu** + `maze.reveal_wall()` ⇒ tường hiện 100% và bàn LUÔN có nghiệm.
#    · `get_cell_text()` không có số · `evaluate_move()` chặn F sớm / hazard `revisit` · `is_dead_end()` coi F như tường ·
#      `on_move_undone()` mở lại ô · `hint_next_cell()` (theo đường mẫu, hoặc DFS cắt tỉa Warnsdorff 60k nút) ·
#      `on_move_blocked()` hiện cảnh báo khi F bị chặn.
# 3. Hạ tầng dùng chung (không phá mode khác): `BaseGameMode.on_move_blocked()` + `hint_next_cell()` (mặc định -1,-1) ·
#    `GridController` ưu tiên hint của mode + KHÔNG vẽ đoạn tường gãy khi hazard `revisit` + gọi `on_move_blocked` ·
#    `GameController._hazard_game_over_reason()` (reason `revisit`) · `gameover_level.gd` hiện tiêu đề theo reason.
# 4. HUD `nodes/hud/one_stroke_hud.tscn` + `OneStrokeHUD` (mockup `matchup_one_stroke.svg`): THỜI GIAN 270×156 (chip
#    "ĐANG TÍNH GIỜ") + BẢNG TIẾN ĐỘ 690×156 (📐 ĐÃ PHỦ KÍN · `x/y Ô` · CÒN LẠI n Ô · ✍️ MỘT NÉT KHÔNG LẶP · chip `p% KÍN` ·
#    thanh tiến độ cắt bằng `clip_contents` · 2 dòng nhắc luật) + `challenge_card() = null`.
#    Art mới: `card_stroke_sheet/row/chip/slider_track/slider_fill/time_chip.svg` · theme thêm 7 variation `HudStroke*`.
#    `GameScene`: `HUD_ONE_STROKE` + map mode · `TOOL_SUB_KEYS["one_stroke"]` · **`TOOL_HIDE_WALL_MODES`** (ẩn nút GHI NHỚ).
# 5. Board: `MazeCell` thêm lớp `Visited` + `VisitedLabel` (`cell_visited_hatch.svg` + `text_game_cell_visited.tres`,
#    nhãn "ĐÃ ĐI" co theo cỡ ô) · `BoardView.refresh_cell_texts()` tự vẽ khi mode có `is_cell_visited()` (ô S/F giữ art gốc).
# 6. Popup hướng dẫn 3 trang: `nodes/popups/instruction/one_stroke.tscn` (121 node) sinh bằng pipeline tool —
#    thêm `one_stroke` vào `MODES` của `tools/mockup/instruction_mockup.py` · ảnh guideline ThorVG-safe
#    (`prepare_guideline_images.py`) · thêm ~40 bản EN vào `tools/content/guide_text_en.py` · `build_instruction_data.py`
#    (74 khoá `STR_GI_ONE_STROKE_*` + 722 dòng GI) · `gen_instruction_popups.py`. **GOTCHA:** 3 ảnh guideline mới tải về
#    có `.import` CŨ thiếu `path=` ⇒ popup báo `Failed loading resource`; sửa bằng cách **xoá `.import` rồi chạy lại
#    `--editor --quit`** để sinh `.ctex`. `GameController.INSTRUCTION_SCENES` thêm `one_stroke`; `HintGuide` thêm
#    `STR_HINT_ONE_STROKE` (chuỗi phải NGẮN để vừa 1 dòng 844px — test đo bằng font thật).
# 7. Kiểm chứng: `test_one_stroke.gd` MỚI **34/34 PASS** · `test_instruction` **326 PASS** (10 popup) · `test_hud_modes`
#    thêm mục **[4d]** · `test_game_scene_integration` + `test_all_game_modes` phủ mode mới · **32 suite: 30 xanh**
#    (2 đỏ là lỗi CŨ: challenge_controller 3 check · hud_modes 7 check — xem vòng 18) · render HUD
#    `tmp_fix/1080x1920_hud_one_stroke.png` khớp mockup (đã soi mắt).
# [ ] CÒN LẠI: gắn `one_stroke` vào `GameManager.DAILY_MODES` (§6 — nhớ cập nhật test ngày→luật) · thêm
#     `matchup_one_stroke` vào `HAND_DRAWN` của `tools/mockup/gen_matchup.py` · art nút VẼ ĐƯỜNG bản bè ngang 680px
#     như mockup (hiện giữ art 360px + ẩn nút GHI NHỚ) · (mode vẫn vào được qua Debug Console / `switch_mode`).

# ----------------------------------------------------------------------------
# WALL BUILDER: MODE + NÚT GỬI BÀI + HUD "BẢNG TƯỜNG ĐÃ VẼ" + POPUP HƯỚNG DẪN 3 TRANG (vòng 20 — 2026-09-19)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("làm tiếp cho tôi Wall Builder"):
# 1. LUẬT (đúng §5.13 + đúng 3 trang mockup hướng dẫn): bàn KHÔNG S/F, không nhân vật, không di chuyển;
#    số tường hiện trên MỌI ô (kể cả 0); kéo nối 2 Anchor để bật/tắt 1 đoạn tường; cần đúng y = Σ(số)/2 đoạn;
#    bấm GỬI: khớp MỌI con số ⇒ THẮNG (không bắt buộc trùng mê cung gốc) · sai ⇒ mất 1 trong 3 LƯỢT GỬI +
#    rung bàn + báo số đoạn lệch · hết 3 lượt ⇒ thua "HẾT LƯỢT GỬI!" (Hồi sinh +1 lượt gửi) · Gợi ý mở + KHOÁ
#    1 đoạn tường thật (không xoá được) · Undo xoá đoạn vừa nối (bỏ qua đoạn khoá) · viền ngoài không vẽ được.
# 2. `scripts/modes/wall_builder_game_mode.gd` (`WallBuilderGameMode`): sinh bàn tường ẩn 100% (`generate(w,h,0.0)`)
#    + LOẠI bàn quá dễ (quá nhiều ô 0/4, thử tối đa 12 lần) · `required_segments` = Σ số / 2 · `evaluate_submit()`
#    trả `{solved, wrong}` với `wrong = Σ|chênh lệch từng ô| / 2` · `can_draw_wall()` (chỉ khe giữa 2 ô thuộc board)
#    · `hint_wall()` + `is_wall_locked()` · `undo_drawn_wall()` · `wall_draw_state()="built"` · `shows_player()=false`.
# 3. Hạ tầng dùng chung (không phá mode cũ): `BaseGameMode` thêm `register_failed_submit()` · `can_draw_wall()` ·
#    `is_wall_locked()` · `on_wall_toggled()` · `undo_drawn_wall()` · `hint_wall()` · `shows_player()` · `revive_desc_key()`;
#    `GridController.handle_anchor_connected()` kiểm tra luật mode trước khi đổi trạng thái rồi báo mode;
#    `GameController.submit_build()` (đúng ⇒ `_on_reached_end()`; sai ⇒ trừ lượt + `shake_board()` + chữ nổi
#    `CÒN LỆCH n ĐOẠN!`; hết lượt ⇒ `_game_over("out_of_submits")`) + `undo()`/`hint()` định tuyến kiểu tường;
#    `GameOverLevelPopup` nhận `revive_desc` + lý do `out_of_submits` (tiêu đề `STR_GAME_OVER_OUT_OF_SUBMITS`);
#    `WallSegment` thêm state **`built`** (xanh lá) · `BoardView` thêm `set_player_visible()`/`shake_board()`.
# 4. HUD `nodes/hud/wall_builder_hud.tscn` + `WallBuilderHUD`: THỜI GIAN 270×156 (chip "ĐANG SUY LUẬN") + BẢNG
#    TƯỜNG ĐÃ VẼ 690×156 (viền xanh lá #2E7D32): 🧱 ĐÃ DỰNG · x/y ĐOẠN · CÒN THIẾU n ĐOẠN · ❤️ LƯỢT GỬI n / 3 LẦN ·
#    chip trạng thái (ĐANG NỐI TƯỜNG / ĐỦ TƯỜNG / SẴN SÀNG GỬI) · thanh tiến độ · 2 dòng nhắc luật; challenge_card()=null.
#    Art mới `card_wall_{sheet,row,chip,slider_track,slider_fill}.svg` · theme thêm `HudWallCoverHead`+`HudWallSubmitRow`.
# 5. Thanh công cụ: `GameScene.TOOL_FULL_KEYS` đổi HẲN nhãn 2 nút thành VẼ TƯỜNG ("Nối tường theo số") + GỬI BÀI
#    ("Kiểm tra kết quả"); `_on_secondary_tool_pressed()` gọi `submit_build()` khi đang ở chế độ này.
# 6. Popup hướng dẫn: `nodes/popups/instruction/wall_builder.tscn` (142 node) qua pipeline tool (thêm `wall_builder`
#    vào MODES + ~35 bản EN + build/gen) → 83 khoá `STR_GI_WALL_BUILDER_*`; chuỗi mới: STR_MODE_WALL_BUILDER ·
#    STR_HINT_WALL_BUILDER · STR_TOOL_DRAW_WALL(_DESC) · STR_TOOL_SUBMIT(_DESC) · STR_HUD_WB_* · STR_WB_WRONG_COUNT ·
#    STR_REVIVE_DESC_SUBMIT · STR_GAME_OVER_OUT_OF_SUBMITS.
# 7. Kiểm chứng: `test_wall_builder.gd` MỚI **47/47 PASS** · `test_instruction` **358 check** (11 popup) ·
#    `test_hud_modes` thêm mục **[4e]** · `test_game_scene_integration` + `test_all_game_modes` + Debug Console phủ mode mới ·
#    **33 suite: 31 xanh** (2 đỏ là lỗi CŨ: challenge_controller 3 check · hud_modes 7 check — xem vòng 18).
# [ ] CÒN LẠI: gắn `wall_builder` (+ `one_stroke`) vào `GameManager.DAILY_MODES` (§6) · thêm 2 mockup vào `HAND_DRAWN` ·
#     tô nền nhạt cho ô đã khớp số (trang trí, mockup có gợi ý) · solver đếm nghiệm để ưu tiên bàn ≥ 2 nghiệm.

# ----------------------------------------------------------------------------
# GẮN 2 MODE MỚI VÀO DAILY + THỬ THÁCH RIÊNG + POLISH ART (vòng 21 — 2026-09-19)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("trừ test trên máy ảo và APK, làm tiếp"):
# 1. VÒNG XOAY DAILY 9 NGÀY (chạy thật): `GameManager.DAILY_MODES` += one_stroke, wall_builder
#    → ngày N = DAILY_MODES[(N-1) % 9]; tên luật trên màn Daily lấy từ `STR_MODE_<ID>` (đã có sẵn).
#    LƯU Ý: bản đồ ngày → luật đổi so với bộ 7 cũ (ngày 13: SƯƠNG MÙ → CHI PHÍ ĐẾM NGƯỢC); test_daily dùng
#    công thức động nên vẫn xanh; các ghi chú/ảnh chụp "ngày nào luật nào" theo bộ cũ phải bỏ.
# 2. `tools/mockup/gen_matchup.py`: thêm `matchup_one_stroke` + `matchup_wall_builder` vào `HAND_DRAWN`.
# 3. THỬ THÁCH THỨ 17 `no_wrong_submit` (Wall Builder): ChallengeTypes + nhánh chấm (đếm `submit_misses`) +
#    chuỗi STR_CHALLENGE_NO_WRONG_SUBMIT / STR_CHALLENGE_ST_SUBMIT_MISSED + CƠ CHẾ BỘ MẶC ĐỊNH RIÊNG THEO CHẾ ĐỘ
#    (`BaseGameMode.default_challenges()` · `ChallengeController.setup_for_floor(..., mode)`); One Stroke =
#    time_max·no_hint·no_undo, Wall Builder = no_wrong_submit·time_max·no_hint (các mode khác giữ bộ chung).
# 4. WALL BUILDER — Ô "ĐÃ KHỚP SỐ" (mockup): `is_cell_satisfied()` + art `cell_satisfied_wash.svg` + node `Satisfied`
#    trong `nodes/game/cell.tscn` + `MazeCell.set_satisfied()`; board tự vẽ lại sau mỗi đoạn tường được nối.
# 5. WALL BUILDER — SOLVER ĐẾM NGHIỆM `count_solutions(maze, cap)`: backtracking theo khe + cắt tỉa
#    (counts ≤ clue ≤ counts + khe còn lại), ngân sách 80k nút → bộ sinh bàn LOẠI bàn quá dễ (số đoạn < cạnh bàn,
#    quá nhiều ô 0/4) và ƯU TIÊN bàn ≥ 2 nghiệm. BẪY ĐÃ DÍNH: chỉ lọc "≥2 nghiệm" thì sinh ra bàn 3×3 chỉ 2 đoạn
#    (test báo "bàn đã đúng sẵn") → phải chấm điểm bàn (ưu tiên nhiều ô phải suy luận) + sàn số đoạn tường.
# 6. ART NÚT CÔNG CỤ THEO MOCKUP: `ToolController.set_mode_textures()` + `btn_tool_path_stroke_active.svg`
#    (One Stroke: nút VẼ ĐƯỜNG ĐI bè ngang 680px) + `btn_tool_submit_normal/pressed.svg` (Wall Builder: GỬI BÀI xanh).
# 7. Kiểm chứng: `test_wall_builder` **53/53** · `test_one_stroke` **35/35** · `test_challenge_types` (17 loại) ·
#    `test_debug_modes` **48 check** (SPECIAL MODES = 9 chế độ) · `test_daily` 87/87 · **33 suite: 31 xanh**
#    (2 đỏ là lỗi CŨ: challenge_controller 3 check · hud_modes 7 check).
# [ ] CÒN LẠI (user để sau): TEST TRÊN MÁY ẢO + BUILD APK cho 2 mode mới; tuỳ chọn: tinh chỉnh vị trí chữ trên nút
#     VẼ ĐƯỜNG ĐI bè ngang cho khớp mockup, bảng xếp hạng riêng cho 2 chế độ.

# ----------------------------------------------------------------------------
# BỎ HẲN CHẾ ĐỘ TIME ATTACK MAZE (vòng 22 — 2026-09-19)
# ----------------------------------------------------------------------------
# [x] ĐÃ LÀM theo yêu cầu user ("Hãy xóa Time Attack Mode ra khỏi game"):
# 1. XOÁ FILE: `scripts/modes/time_attack_game_mode.gd` (+.uid) · `scripts/nodes/hud/time_attack_hud.gd` (+.uid) ·
#    `nodes/hud/time_attack_hud.tscn` · `nodes/popups/instruction/time_attack.tscn` ·
#    `assets/images/instructions/mode_time_attack.svg` (+import) ·
#    `assets/images/instructions/guideline_image/image_instruction_time_attack_page{1,2,3}.svg` (+import).
# 2. NỐI DÂY: `GameModeController` (case time_attack) · `GameScene` (HUD_TIME_ATTACK + 2 map) · `HintGuide` ·
#    `GameController.INSTRUCTION_SCENES` + NHÁNH `if mode is TimeAttackGameMode` trong `start_new_run()` → đồng hồ giờ luôn
#    đếm lên (`TimerController.start_countdown()` thành API mồ côi — giữ lại phòng khi cần đếm ngược ở chế độ khác).
# 3. VÒNG XOAY DAILY còn 8 chế độ: [minesweeper, sum_path, countdown_cost, blind_memory, fog_of_war, fading_ink,
#    one_stroke, wall_builder] → ngày N = DAILY_MODES[(N-1) % 8]. LƯU Ý: mọi ngày DỊCH LÊN 1 BẬC (ngày 1 trước là
#    TIME ATTACK, nay là MINESWEEPER); save cũ không hỏng.
# 4. TOOL: bỏ entry time_attack khỏi gen_matchup.py (MODES + dump) · instruction_mockup.py (MODES) ·
#    level_designer/app/config.py (MODE_IDS) → chạy lại pipeline: CSV còn 730 dòng STR_GI_* (bỏ 75 khoá
#    STR_GI_TIME_ATTACK_*), generator sinh 10 popup (không sinh lại time_attack.tscn).
# 5. TEST: test_hud_modes (bỏ mục [4b]) · test_instruction (326 check / 10 popup) · test_all_game_modes (8 modes) ·
#    test_debug_modes (8 chế độ Special — ván test đổi sang minesweeper) · test_game_scene_integration · dev_aspects.
# 6. GIỮ LẠI CÓ CHỦ Ý: mockup trong `mockup/` + bản dịch trong `guide_text_en.py` (tài liệu lịch sử, không vào build) ·
#    khoá STR_MODE_TIME_ATTACK / STR_HINT_TIME_ATTACK trong CSV (để save cũ không hiện khoá thô).
# 7. Kiểm chứng: test_hud_modes 7/146 FAIL — VẪN ĐÚNG 7 lỗi CŨ (Sum Path/Fading Ink HUD), không phát sinh lỗi mới ·
#    test_instruction 326 PASS · test_debug_modes 46/46 · test_daily 87/87 · test_all_game_modes 14/14 · **33 suite: 31 xanh**.
# [ ] CÒN LẠI: test máy ảo + build APK (user để sau) · quyết định có xoá luôn mockup/lịch sử Time Attack không · 2 chế độ
#     mới cần thêm vào `tools/level_designer/app/config.py` nếu tool cần dùng.


# ============================================================================================
# VÒNG 27: TÁCH LAYOUT NGANG CHO CÁC MÀN CÒN LẠI — MÀN CHƯƠNG ĐẦU TIÊN (mẫu) — 33 suite: 31 xanh · harness chapters+main 10/10
# ============================================================================================
# 1. TOOL MỚI `tools/ui/split_screen_layouts.py <màn> [...]`: tách 1 màn thành
#    `scenes/orientation/portrait/<màn>.tscn` (bản dọc giữ NGUYÊN bố cục, root KẾ THỪA scaffold portrait.tscn)
#    + `scenes/<màn>.tscn` chỉ còn root + `Portrait` (+ `Landscape` nếu bản ngang đã tồn tại). Bỏ `uid`/`unique_id` khi copy.
# 2. BASE: thêm `BaseScene.ui_path("A/B")` (tra node theo đường dẫn tương đối trong layout ĐANG hiển thị) —
#    2 layout PHẢI đặt trùng tên node; KHÔNG dùng được %UniqueName.
# 3. MÀN CHƯƠNG (mẫu): `scenes/orientation/landscape/chapters.tscn` — cột TRÁI (TopBar · Ví Sao · Banner hướng dẫn ·
#    CTA chân trang) + cột PHẢI lưới thẻ chương **GridContainer 2–3 cột**; `chapters.gd` → `_bind_refs()` dùng `ui_path`
#    + nối `orientation_changed` → bind lại khi xoay + `_apply_grid_columns()` (số cột = `int(bề rộng List / 1040)`).
# 4. 9-SLICE: `tools/ui/menu_buttons_9slice.py` thêm bộ `cta_*` (art ở `assets/images/common`) + nút `ContinueButton` +
#    chống chạy lại (idempotent) + sửa lỗi console Windows (reconfigure UTF-8) — 18 StyleBoxTexture trong
#    `resources/settings/styles/menu/`; nút CTA ở layout ngang = `Button` (không phải TextureButton nữa).
# 5. QUY TẮC ART (user chốt): art KHÔNG 9-slice được cho layout ngang → vẽ SVG mới trong `assets/images-landscape/`
#    (giữ cấu trúc thư mục con như `assets/images/`). Ví dụ sẽ cần: thẻ màn chơi (level card), bục xếp hạng, thẻ cửa hàng…
# 6. TEST: mọi lookup trong test phải đi qua layout (`scene.ui_path("TopBar/Back")`) — test_chapters **243/243 PASS** ·
#    harness `dev_aspects.gd -- --only=chapters,main --shots=1` **10/10 OK** (16:9 lưới 2 cột · 19.5:9 3 cột · 4:3 1 cột).
# [ ] CÒN LẠI (thứ tự đề xuất): levels (số thẻ/trang + lưới động) → settings (2 cột) → daily → shop → ranking →
#     archivement → splash/title/credit/debug → game (màn chơi). Mỗi màn: tạo layout ngang → chạy tool tách →
#     refactor script sang `ui_path`/`ui()` + `orientation_changed` → bổ sung 9-slice hoặc art `images-landscape/` →
#     sửa test + chạy harness 10 tỉ lệ.


# ============================================================================================
# VÒNG 25: NỀN TẢNG ĐA TỈ LỆ THEO guide/GUIDE.MD + MAIN SCENE 2 LAYOUT (2026-02) — 33 suite: 31 xanh
# ============================================================================================
# 1. BASE (scripts/scenes/base.gd): 2 layout con cùng cấp `Portrait`/`Landscape` (instance
#    scenes/orientation/<hướng>/<màn>.tscn — KẾ THỪA scaffold <hướng>/portrait|landscape.tscn) — tự bật/tắt theo hướng canvas + signal
#    `orientation_changed(is_landscape)`. Khung nội dung: dọc = clamp(canvas.x,1080,1440) canh giữa ·
#    ngang (có layout ngang) = PHỦ KÍN canvas · màn chưa tách layout = giữ cột 1080 như cũ.
#    Thêm `ui(tên)` / `ui_child(cha, con)` — KHÔNG dùng được %Unique vì 2 layout trùng tên node.
# 2. HELPER: `scripts/utils/auto_fit_label.gd` (AutoFitLabel) — chữ tự co vừa khung.
# 3. MAIN SCENE: `scenes/orientation/portrait/main.tscn` (giữ nguyên bố cục dọc) +
#    `scenes/orientation/landscape/main.tscn` (tờ giấy NinePatchRect + 2 cột Hero/Menu) ·
#    thẻ chế độ + nút công cụ chuyển sang `Button` + StyleBoxTexture 9-slice (`tools/ui/menu_buttons_9slice.py`)
#    · scenes/main.tscn chỉ còn root + 2 layout con.
# 4. SỐ ĐO QUAN TRỌNG: màn NGANG có canvas CAO ĐÚNG 1920 → cỡ chữ/khung layout ngang = spec × 1.778
#    (16:9 → canvas 3413×1920 · 4:3 → 2560×1920 · 19.5:9 → 4160×1920; 3:4 dọc → 1440×1920). In lại bằng
#    `--rendering-driver opengl3 --script res://scripts/test_case/dev_size_probe.gd`.
# 5. TEST: `test_main_layout` cập nhật theo 2 layout · `dev_aspects.gd -- --only=main --shots=1` 10/10 OK
#    (ảnh: `tmp_aspect/<WxH>/main.png`) · full 33 suite: 31 xanh (2 đỏ CŨ: Sum Path/Fading Ink HUD).
# [ ] CÒN LẠI: art thẻ chế độ bị KÉO NGANG ở layout ngang (1848×414 vs art 804×310) → cần art ngang riêng hoặc
#     StyleBoxTexture 9-slice · các màn khác chưa tách layout ngang (xem GUIDE §6) · chưa áp safe-area tai thỏ ·
#     test máy ảo/APK.

